packet:            M2R — Context Bars (+memory category)
session:           codex / 2026-08-04 / 8a4e
status:            DONE
what exists now:   Harness records the latest completed provider request for
                   each thread. The used total comes from that terminal
                   response's per-request input tokens; capacity comes from
                   the immutable resolved model context length. The public
                   Rack query exposes truthful CURRENT and GLOBAL snapshots.

                   The first-party Context Bars module sits compactly beside
                   Palace Vitals. Its bar and table show System, History,
                   Memory, and Tools; its copy explicitly distinguishes the
                   estimated split from the measured total, draws the passive
                   80% line, and says compaction is not active. Before a first
                   response it shows a waiting state. Mobile collapses to the
                   measured used/capacity reading without covering Chat.

                   The required customer-copy sweep removed implementation
                   language from Memory tuning, the Memory Gate, and Model
                   controls while leaving internal protocol values stable.
                   Harness commit 1887756 contains Decision 031, the tracker,
                   public query, Rack module, built assets, tests, SOP, and
                   rendered evidence.
deviations:        Garden A-039 and Harness Decision 031 (P2.2, P2.5) complete
                   the provider-silent category measurement and threshold
                   semantics. The broker does not report a trustworthy split,
                   so the request total and limit are measured while the four
                   categories are explicitly estimated and normalized back to
                   the measured total. The 80% line is presentation-only; no
                   M3 compaction behavior was pulled forward. No vision
                   deviation.
evidence:          Full Harness ground passed 601 tests with three live Spine
                   contract tests excluded exactly as at boot; full Spine
                   ground passed 208 tests through Colima/Testcontainers.
                   Ruff passed in both repos. Web ESLint, six unit tests,
                   TypeScript, and the Vite production build passed.

                   The isolated production Rack was exercised at 1440x900 and
                   390x844 in a fresh browser pass. Desktop proves the four
                   categories, compact table, exact 102.4K / 128K aggregate,
                   80% line, inactive-compaction disclosure, and GLOBAL scope.
                   Mobile proves the same aggregate remains visible in the
                   collapsed right-hand strip. Both clean runs recorded zero
                   console warnings or errors. Screenshots and the owner SOP
                   are in `harness/verification/m2r/`.
notes to the next agent:
                   M2N remains blocked only on owner decision F015; do not
                   invent restore data-loss semantics. D3 remains blocked on
                   F014. Run a fresh boot sequence; M2Q is now the first
                   dependency-ready TODO row by board order. Context category
                   values are estimates by enacted contract; do not relabel
                   them as provider facts or turn the passive threshold into
                   compaction machinery before M3.
