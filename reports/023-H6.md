packet:            H6 — Memory panel
session:           codex / 2026-07-28 / 86af
status:            DONE
what exists now:   Harness commit 1b2311e adds the typed bidirectional
                   memory.panel.update seam, daemon-owned principal and
                   provenance, a complete ACTIVE-memory panel, frozen
                   per-thread injection state, serialized mid-thread removal,
                   persistent panel-removal tool exclusions, and explicit
                   edit/pin CAS-conflict recovery. The product renders as a
                   restrained desktop rail and a focus-contained 390×844
                   drawer. Commit 1ee9b22 records the exact-final-build Chrome
                   evidence and deterministic deployed-Spine traces.
deviations:        Garden A-024 completes the previously unspecified C.7 H6
                   payload and authority boundary. Harness DECISIONS 014
                   [P1.2.1d, P1.3] adopts that law; Decision 015 [P3] records
                   the live-use correction from a 32px composer textarea to a
                   measured 44px target. No glossary entry is proposed.
evidence:          Harness verification/h6/: desktop trace PASS at 62 records,
                   mobile trace PASS at 72 records, ten true-PNG screenshots,
                   a 5m10s unscripted final-build Chrome segment, and four
                   exact cleanup receipts with six unique tombstones and zero
                   remaining ACTIVE IDs each. The final commit showed correct
                   next-call removal, frozen edit/pin behavior, principal
                   isolation, visible 409 replacement without retry, refresh
                   and reload recovery, no 390px overflow, 44px minimum
                   controls, focus wrap/return, and zero console warnings or
                   errors. Independent review returned READY. Harness:
                   293 passed, 2 deselected; Spine: 160 passed. Ruff,
                   formatting, lock checks, repository scope fence, web
                   lint/build, and diff checks passed.
notes to the next agent:  H8 is now the next deterministic eligible packet;
                   the H6 ⊗ H8 exclusion clears with this DONE handoff. H5's
                   HUMAN USE HOLD remains and gates J only, so do not clear it
                   or delay H8/H9/I1 on it. Preserve the intentional lifetime
                   split: initial gate removals exclude tools for the first
                   run only, while a successful panel mid-thread removal is
                   persistent for that daemon-lifetime thread. Edit and pin
                   change future injection candidacy but never rewrite the
                   frozen open-thread block. Recorded friction is limited to
                   mutation-driven card reordering, a dense scrollable desktop
                   rail, and Escape closing the whole phone drawer while
                   safely discarding an editor draft.
