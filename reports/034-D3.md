packet:            D3 — Deploy & onboarding command
session:           codex / 2026-08-01 / d3a1
status:            BLOCKED
what exists now:   `nocturne-ai==0.1.0` is the Harness distribution and its
                   console script remains `nocturne`; `nocturne-spine==0.1.0`
                   is its exact lockstep dependency. Both repositories build
                   constrained sdists and deterministic wheels, carry split
                   OIDC release workflows, and are pushed. The `nocturne`
                   splash repository now holds the one-key quickstart with the
                   exact `pipx install nocturne-ai` install line. An isolated
                   installed-wheel run reached a working browser chat.
deviations:        Harness Decisions 020 and 021 [P4]: installed-wheel local
                   control plane and fixed-foundation state-aware cloud
                   reconciliation. No contract deviation; both decisions
                   implement ADR-019 and D.2 045/073 literally.
evidence:          Spine fdb817619e9ae1d5135a696a8775abfbe7d3324b;
                   Harness 5289528209f6bc6a46fcb1f02cec73f6f0cddb6d;
                   splash 3afc875970da6c0f2f721dd52a36633ecd65582a.
                   Harness: 527 passed / 2 contract tests deselected, Ruff,
                   format, lock, scope fence, web lint/build, and committed-web
                   diff all green. Spine: 167 passed with Docker plus Ruff,
                   lock, and scope fence green. Direct wheels match wheels
                   rebuilt from their sdists byte-for-byte; Twine passes all
                   artifacts; Harness sdist scope passes at 30 entries.
                   `harness/verification/d3/` contains the installed-wheel
                   browser acceptance screenshot and complete redacted cloud
                   dry-run. The final dry-run printed all 20 stages without
                   mutation: exact D2 and security topology, safe forward work,
                   and a fail-closed database/migration mismatch. PyPI returned
                   404 for both project JSON endpoints at handoff.
notes to the next agent:
                   F014 is the only D3 completion gate. The owner first creates
                   the two pending PyPI publishers named in FLAGS.md. Do not
                   create tags/releases before that setup. Then release Spine
                   `v0.1.0`, wait for its public index artifact, release Harness
                   `v0.1.0`, and run a truly clean public-index pipx install plus
                   the one-key browser acceptance. Preserve publish order: the
                   Harness metadata requires `nocturne-spine==0.1.0`.

                   The cloud dry-run exposed an unsupported `gcloud artifacts`
                   flag on its first attempt; Harness 5289528 removes it and
                   adds the live-derived regression test. Its second run is the
                   recorded evidence. Do not treat the reported database/user/
                   secret/Alembic disagreement as D3 mutation authority. The
                   planner correctly refuses adoption, rotation, replacement,
                   and deletion; reconciling that live D1 state needs a separate
                   owner decision. The temporary verification container/network
                   was removed; its isolated Docker volume was preserved.

                   Independent M1C/M2 relays remain claimable. When the public
                   proof passes, resolve F014, set D3 DONE with the public PyPI
                   URLs and workflow-run receipts, commit/push Garden, and stop.

GATE ERRATUM (human gate, 2026-08-01; D.2 078): this report's evidence
overstated one claim — "the live Alembic head was proven incompatible."
It was not inspected: the drift classification arose from the db/user/
secret tuple mismatch before alembic_version was ever read (agent
self-reported in the packaging review). The deploy classifier's behavior
was correct; the wording was not. Recorded per the never-drop-decisions
discipline; no product change required.
