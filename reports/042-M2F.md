packet:            M2F — The Chrysopoeia learner v1
session:           codex / 2026-08-03 / c6a1
status:            DONE
what exists now:   Spine exposes bearer-protected, bodyless POST `/retrain`.
                   One repeatable-read operation serializes on a database
                   advisory lock, removes whole fixture/test/verification
                   gates, chronologically holds out the newest gates, and
                   exactly refits the full older log with deterministic
                   squared pairwise hinge loss. Six global weights remain
                   non-negative and sum to one; per-memory offsets are L2
                   shrunk and remain separate from online never-kill bias.
                   The binary referee counts each held-out disposition,
                   discounts only passive survival, excludes wrong/stale,
                   applies the configured real-win margin, and uses injected
                   body tokens only as the exact cheaper-at-tie rule. A winner
                   becomes one inactive content-addressed scorer_config row;
                   the proposal preserves every manual tau/budget/k/decay/pool
                   parameter and carries its digest, split, settings, fit,
                   replay scores, and learned offsets. Identical reruns are
                   idempotent. Optional SPINE_LEARNER_SCHEDULE_HOURS invokes
                   the same service; unset remains manual-only.
deviations:        Garden A-031 and Spine Decision 025 [P1.2] complete the
                   operational shapes ADR-005 deliberately left open: exact
                   hygiene grammar, whole-gate 80/20 default split, 25-signal
                   first-challenger floor, 0.25 passive discount, 0.05 pair
                   margin, 1.0 bias shrinkage, 1.0 replay win margin, proposal
                   manifest, and `/retrain` response. A final actor audit
                   corrected a subtle M2G interaction before close: explicit
                   mid-thread feedback stays full-weight even when it replaces
                   an event originally stamped actor_class=passive. No schema
                   migration, learner UI, activation endpoint, automatic
                   activation, online SGD, model-class ladder, or heavyweight
                   numeric dependency was added. Budget and k remain manual
                   and outside learner replay exactly as v2.34 requires.
evidence:          Spine 326de29. Final locked suites: Spine 195 passed;
                   Harness 577 passed / 3 live-contract tests deselected; both
                   `ruff check` passes clean; committed OpenAPI freshness and
                   bearer behavior pass. `spine/verification/m2f/README.md`
                   maps every exit criterion to deterministic and live-
                   Postgres tests: whole-gate time split, simplex fit,
                   shrinkage, hygiene, passive/full-weight classification,
                   binary margin and cheaper tie, proposal inactivity,
                   content-addressed idempotence, later-active offset loading,
                   and scheduler shutdown. This packet has no user-facing
                   surface and makes no provider call; browser or synthetic
                   model evidence would not test its actual authority boundary.
notes to the next agent:
                   M2H is next by deterministic board order; do not claim it
                   in this session. M2L and later independent rows are also
                   dependency-ready but remain later. D3 stays blocked only
                   on owner creation of the two PyPI trusted publishers.
                   Preserve `_learner` as immutable proposal provenance and
                   do not let M2H's extraction queue become training signal by
                   accident. Owner activation remains future console work;
                   `/retrain` intentionally cannot flip the active row.
