packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / c4a8
status:            RETURNED_TODO
what exists now:   Spine commit 475513b measures the logical Palace database
                   size inside both canonical Vitals transactions and exposes
                   the remaining Harness-owned observations as explicitly
                   partial. Harness commit a1cb191 enriches both GLOBAL and
                   CURRENT Rack Vitals with daemon RSS, monotonic uptime, free
                   and total disk, journal bytes, and backup bytes.

                   Doctor and startup now share one symlink-ignoring local
                   resource reader and the A-043 early-warning boundary.
                   `nocturne up` checks before Docker pull/start, prints one
                   owner-language warning when space is low, and continues
                   without a prompt. Palace Vitals renders the measurements as
                   a passive collapsed summary and expanded gauge row; partial
                   observations remain unavailable rather than zero.

                   M2N remains incomplete. The literal informed side-by-side
                   restore flow and the owner-cloud pre-migration backup
                   receipt are still untouched and require their own slice.
deviations:        Garden A-044 completes the previously unspecified resource
                   wire shape, observation split, shared warning boundary, UI
                   attention behavior, and executable soak ceiling. Harness
                   Decision 036 and Spine Decision 029 record why observation
                   is split at the process boundary. No vision deviation and
                   no new glossary term.
evidence:          Fresh boot ground passed before the slice: Harness 614
                   passed / 3 live contract tests deselected; Spine 220 passed;
                   web ESLint, 6 Node tests, TypeScript, and Vite build passed.
                   An initial Harness command accidentally included the three
                   live contract tests and produced only missing-environment
                   errors; the required local lane was then run correctly.

                   Post-change: Harness 618 passed / 3 live contracts
                   deselected; Spine 221 passed with the mandated Colima
                   socket override; web ESLint, 7 Node tests, TypeScript, and
                   Vite build passed. Ruff lint passed in both repos and all
                   changed Python files are Ruff-formatted. Motivation checks
                   passed: Harness 341 discovered / 308 grandfathered; Spine
                   154 discovered / 146 grandfathered. Committed OpenAPI is
                   current.

                   `verification/m2n/resources-soak.md` records 500 warm-ups
                   plus 10,000 sequential real Rack Vitals HTTP queries. Peak
                   and final RSS growth were both 1,916,928 bytes against the
                   33,554,432-byte ceiling. Browser evidence at 1440x900 and
                   390x844 confirms the expanded passive resource gauges and
                   collapsed `100.0 GiB free · DB 7.5 MiB` summary; the fixture
                   identifies itself visibly and browser/server cleanup ran.
notes to the next agent:
                   Run a fresh boot; M2N remains first eligible by board order.
                   Do not reopen resource observation, startup warning, doctor,
                   or backup-retention law unless fresh evidence finds a defect.

                   Restore remains literal v2.49: restore to a fresh side-by-
                   side volume, verify there, compute the named rollback
                   manifest, require explicit confirmation, switch while
                   retaining the former volume, and never touch live state on
                   failure. Keep the next slice uninterrupted because it owns
                   the data-loss boundary.

                   The other remaining seam is
                   `GcloudDeployBackend._apply_migrations`: create and persist
                   the specified Cloud SQL pre-migration backup receipt before
                   that call without claiming automated cloud restore. Do not
                   begin M2Q or M2P.
