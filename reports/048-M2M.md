packet:            M2M — Ledger self-audit vs broker
session:           codex / 2026-08-03 / d91c
status:            DONE
what exists now:   Spine runs one advisory-locked broker reconciliation at
                   owned-process startup and every 24 hours thereafter. It
                   reads cumulative usage from OpenRouter's current-key
                   endpoint with the already-configured bearer, sums priced
                   llm.request and llm.embedding receipts, counts unpriced
                   lines, and appends an immutable audit observation.

                   The first successful observation fixes a cumulative
                   baseline. Later observations compare only broker and
                   ledger growth since that baseline, preserving M2A's
                   authority without mislabeling pre-ledger broker spend as
                   drift. Exact 12-decimal USD arithmetic and a default
                   $0.000001 tolerance classify each run as baseline,
                   balanced, drift, or unavailable. Network and response
                   failures persist only stable safe codes and never block
                   receipts, chat, embeddings, Vitals, or the next run.

                   Both Vitals scopes now carry the latest Palace-wide
                   reconciliation through strict Spine, Harness, and web
                   contracts. The rack renders baseline, aligned, signed
                   drift, temporary unavailability, or not-recorded as one
                   compact line. Drift uses the existing --danger token with
                   no alert role, popup, notification, card, or new attention
                   channel; the line remains visible when Vitals collapses.
deviations:        Garden A-037 freezes the credential boundary, endpoint,
                   cumulative baseline, tolerance, immutable row, cadence,
                   safe failures, and Vitals projection omitted by ADR-024.
                   The official current-key endpoint exposes cumulative usage
                   to the existing bearer. The account credits and activity
                   endpoints require a management key, so M2M deliberately
                   does not use them and preserves the two-secret owner path.
                   Direct-provider embedding overrides remain honest
                   not_recorded. No GCP billing export or cloud resource was
                   read or created.
evidence:          Spine eaf5bc7 and Harness 38720ae are pushed to origin/main.
                   Spine's locked Colima suite passes 208 tests and Ruff.
                   Harness passes 590 non-live tests and Ruff; its three
                   external live-contract tests remain intentionally excluded
                   because this session has no SPINE_URL/SPINE_TOKEN. Web
                   ESLint, six unit tests, TypeScript, and Vite build pass.

                   Focused Spine acceptance passes 26 migration, schema,
                   reconciliation, and Vitals tests. It proves baseline,
                   balanced, drift, safe-unavailable, append-only mutation
                   rejection, current-key parsing, startup-immediate schedule,
                   exact Vitals projection, and direct-provider disablement.

                   The visibly bannered 1440x900 fixture under
                   Harness verification/m2m rendered the signed drift in both
                   expanded and collapsed production rack states. DOM/style
                   inspection found exactly one drift line, color
                   rgb(255, 64, 95), no alert role, and zero alert elements.
notes to the next agent:
                   M2N is next by deterministic board order; do not claim it in
                   this session. M2M intentionally audits only the configured
                   OpenRouter key's cumulative usage against LLM ledger rows.
                   GCP billing export remains deferred and owner-gated. D3
                   remains blocked solely on the owner-created PyPI trusted
                   publishers.
