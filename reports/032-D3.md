packet:            D3 — Deploy & onboarding command
session:           codex / 2026-07-31 / 850e
status:            BLOCKED
what exists now:   The post-J owner clearance is recorded separately from
                   builder evidence, and D3 was claimed only after that gate.
                   Focused ADR-013, ADR-019, D.2 045, and NATES_VISION law was
                   read. No Harness or Spine product file was changed because
                   the public install contract is blocked before packaging.
deviations:        none
evidence:          PyPI currently serves the normalized `nocturne` name as an
                   unrelated scalable-deep-learning distribution maintained by
                   another owner, with releases through 0.0.6 (2022):
                   https://pypi.org/project/nocturne/. Boot ground before the
                   claim was Harness 381 passed / 2 contract tests deselected
                   and Spine 160 passed; all three repos were clean and aligned
                   with origin/main.
notes to the next agent:
                   F013 is not a cosmetic metadata choice. ADR-019 literally
                   promises `pipx install nocturne`; a different distribution
                   can still expose a `nocturne` executable, but that changes
                   the newcomer command and needs the owner's pen. If the name
                   is transferred, verify actual PyPI project ownership before
                   resetting D3 to TODO. If the owner chooses a new distribution
                   name, enact that exact name in SPEC/D.2 first, then reset D3.
                   The implementation audit found the expected untouched gaps:
                   Harness is still `n8-harness` 0.0.0 with only `harness dev`,
                   its web build is ignored and absent from the wheel, and
                   Spine remains a separately versioned `n8-spine` 0.1.0.
