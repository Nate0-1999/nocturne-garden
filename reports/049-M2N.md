packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-03 / b4d7
status:            BLOCKED
what exists now:   No product implementation was started. The relay claimed
                   M2N, verified the inherited ground, read the full packet
                   law, and stopped at the first contract boundary that PLAN
                   §2 forbids an agent from deciding.

                   F015 records the unresolved restore semantics. The packet
                   requires `nocturne restore`, but ratified law never says
                   whether restore replaces the live Palace, requires an
                   empty target, or restores beside it and switches after
                   verification. Those are materially different recovery and
                   data-loss contracts, not an incidental CLI detail.
deviations:        None. No qualifying amendment can close this gap because
                   PLAN §2 excludes data-loss semantics from COMPLETION.
                   F015 recommends a non-destructive side-by-side restore
                   that preserves the prior volume until explicit owner
                   confirmation, and asks whether owner-cloud restore is in
                   M2N or remains a human Cloud SQL operation.
evidence:          Before the claim, current Garden, Spine, Harness, and web
                   ground was clean and green. Spine passed 208 tests and
                   Ruff under the locked Colima command. Harness passed 590
                   non-live tests and Ruff; the three externally configured
                   live-contract tests remained intentionally deselected.
                   Web ESLint, six unit tests, TypeScript, and Vite build
                   passed. Garden claim commit 1c2c210 is on origin/main.
notes to the next agent:
                   Do not reset M2N or implement its product changes until
                   the owner resolves F015 and returns the row to TODO. After
                   resolution, run the Boot Sequence and take current ground;
                   all implementation charges remain outstanding. M2O is the
                   next deterministic claimable packet while M2N is blocked.
