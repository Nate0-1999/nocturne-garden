packet:            H8 — Gate-day polish: markdown + model visibility
session:           codex / 2026-07-30 / 86af
status:            DONE
what exists now:   Harness commit abb4617 renders a sanitized, theme-token
                   Markdown subset for assistant messages while leaving user
                   messages plain; carries the daemon-resolved model as an
                   optional per-thread snapshot/start extension and shows it
                   in the header; and makes /remember generate a typed label
                   plus 2–5 validated keywords in one tools-free completion.
                   A local SVG favicon closes the baseline console request.
deviations:        Harness DECISIONS 016 [P2, P3] records the renderer,
                   thread-owned model truth, keyword draft, and favicon
                   choices. A-021 already supersedes H8's stale A-020
                   MODEL_INTELLIGENCE_FLOOR parenthetical, so H8 exposes the
                   truthful static M1 resolution seam and does not build H9's
                   policy resolver or the M2 selector. No amendment or
                   glossary entry is proposed.
evidence:          verification/h8/ contains the repeatable production-SPA
                   browser check, six passing deterministic/deployed-Spine
                   traces (18/18/18/22/18/22 records), six exact cleanup
                   receipts with six unique tombstones and zero remaining
                   exact ACTIVE IDs, and 27 genuine PNG screenshots. The
                   screenshot-complete live Chrome rerun records every
                   desktop, exact 390×844 phone, and unscripted 320px step in
                   first person; no product action used a direct protocol
                   call or state injection. Independent code and evidence
                   audits returned clean after the reviewer-requested
                   repeatable rendered regression, second-save guard,
                   per-step screenshots, and PNG-format correction. Harness:
                   304 passed, 2 deselected. Ruff, formatting, lock, web
                   build/lint, Node syntax, diff, trace, cleanup, dimension,
                   reference, and port-closure checks passed.
notes to the next agent:  H9 is now the next deterministic eligible packet.
                   H5's HUMAN USE HOLD remains and gates J only; do not clear
                   it or delay H9/I1 on it. Preserve H8's optional,
                   backward-compatible resolved_model wire field, but replace
                   each thread's initial static value with H9's enacted
                   A-021 policy result. /remember must remain one metadata
                   request and must fail before persistence when 2–5 distinct
                   nonblank keywords do not survive normalization. Tooling
                   note: npm install reported one high-severity advisory while
                   the later offline audit reported zero; online advisory
                   detail was not retrieved after the audit-upload permission
                   was denied. Do not hide or auto-fix that discrepancy;
                   refresh it only through an authorized registry audit.
