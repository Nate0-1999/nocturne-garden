packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / a93f
status:            RETURNED_TODO
what exists now:   F015's v2.49 resolution made M2N executable. Harness commit
                   545eb5c pins the local pgvector image by multi-platform OCI
                   digest and introduces the first preserving config migration:
                   private version-1 owner config upgrades atomically to
                   version 2 with bounded backup-generation retention.

                   Spine commit fa47ab8 serializes every online packaged
                   Alembic run with one database session advisory lock. A real
                   Postgres matrix now proves every supported historical
                   revision, 0001 through 0009, upgrades to the single head.
                   Law-coverage reports in both repos include the new motivated
                   tests.

                   M2N is not complete. `nocturne backup`, informed side-by-side
                   `restore`, and `doctor`; pre-migration backup receipts for
                   local and owner-cloud paths; generation pruning; resource
                   Vitals; and the daemon soak bound remain unbuilt. The new
                   retention config is intentionally inert until backup owns it.
deviations:        Garden A-041 [P1.3, P4], Harness Decision 033, and Spine
                   Decision 028 complete the version-1-to-2 config transition,
                   retention range, shared advisory-lock identity, immutable
                   database image identity, and current supported-revision set.
                   No vision deviation and no new glossary term.
evidence:          Harness: 608 passed, 3 live contract tests deselected; Spine:
                   220 passed through Colima/Testcontainers. Web ESLint, 6 Node
                   unit tests, TypeScript, and Vite build passed. Full Ruff lint
                   passed in both repos. Focused Harness lifecycle tests: 5;
                   focused Spine lock/matrix tests: 10. Motivation checks pass
                   at 332 discovered / 312 grandfathered Harness tests and 153
                   discovered / 147 grandfathered Spine tests. The pinned OCI
                   index digest was resolved from `pgvector/pgvector:pg16` with
                   Docker Buildx on 2026-08-04.
notes to the next agent:
                   Run a fresh boot; M2N remains first eligible by board order.
                   Continue from A-041 rather than redesigning config or locks.
                   Build backup first, because restore, automatic migration
                   receipts, pruning, and doctor all need one receipt/archive
                   authority. Then implement v2.49 restore literally: fresh
                   volume, restore and verify there, compute the named rollback
                   manifest, explicit confirmation, switch only afterward,
                   retain the former volume, and never touch live state on
                   failure. The owner-cloud migration path is
                   `GcloudDeployBackend._apply_migrations`; it already calls the
                   same locked packaged entrypoint but still needs its backup
                   receipt before that call. Finish doctor/resources/soak, run
                   all gates, and only then close M2N. Do not begin M2Q or M2P.
