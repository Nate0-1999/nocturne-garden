packet:            M2J — Parameter registry + model device
session:           codex / 2026-08-03 / 3d91
status:            DONE
what exists now:   Harness owns one typed parameter registry for the six
                   thread model-request descriptors: slug, temperature,
                   top_p, top_k, max_tokens, and effort. Nullable values mean
                   provider inheritance. Accepted writes are validated against
                   the rack manifest binding, journaled as parameter.change,
                   replayable at an aware as_of time, and applied to the next
                   fresh model request without disturbing session identity,
                   provider sorting, or model-stickiness epochs. Selector
                   changes additionally use the named resolver and publish the
                   existing model.change truth.

                   MODEL DEVICE is a real control rack module, not decorative
                   chrome. It exposes the resolved route, selector, four
                   numeric controls, effort, per-control inheritance, CURRENT
                   versus read-only Defaults scope, and a read-only history
                   scrubber. The Chat header consumes the same model.change
                   event, so the control surface and active-model label cannot
                   present split truth.
deviations:        Garden A-034 closes the exact registry contract omitted by
                   PLAN and ADR-023 clause 3: descriptor bounds, nullable
                   inheritance, public rack query/write resources, manifest
                   binding at both host and daemon, accepted/refused event
                   shapes, daemon-lifetime replay, and read-only global
                   defaults. It does not resurrect A-020's superseded model
                   floor, invent a persisted global profile, or add M3 policy,
                   presets, account sync, arbitrary plugin writes, or a second
                   model-routing path.

                   The first automated walkthrough exposed a real action-order
                   race: an unrelated asynchronous control refresh could
                   overwrite an unresolved selector draft. The device now
                   keeps draft ownership until resolve or history navigation;
                   the verifier also waits for each accepted history entry.
                   Manual inspection separately caught and fixed stale Chat-
                   header truth and an over-broad historical query guard before
                   final evidence was captured.
evidence:          Harness da5c220 is pushed to origin/main. Final locked
                   suites: Harness 586 passed with separately deployed live-
                   contract tests ignored; Spine 199 passed through Colima;
                   Ruff passes. Web ESLint passes, six unit tests pass, and the
                   TypeScript/Vite production build passes with committed dist.

                   Registry tests prove unknown, unbound, and invalid writes
                   cannot mutate state; accepted values and historical
                   replay remain coherent; selector resolution preserves the
                   other parameters; and all five broker request settings are
                   forwarded into a fresh model settings body. The visibly
                   bannered deterministic fixture passed at 1440x900 and
                   390x844 with synchronized route truth, accepted temperature
                   and effort writes, named selector resolution, read-only
                   historical replay, read-only Defaults, and no horizontal
                   overflow. A hostile write from module chat returned 403 and
                   produced a durable value-free parameter.refused/unbound
                   event. Screenshots, trace, checker, and SOP are committed
                   under verification/m2j/.
notes to the next agent:
                   M2K is next by deterministic board order; do not claim it in
                   this session. Preserve MODEL DEVICE as the sole public model
                   control surface and consume registry/model event truth
                   rather than copying model state into the Memory Graph.
                   D3 remains blocked solely on the owner-created PyPI trusted
                   publishers.
