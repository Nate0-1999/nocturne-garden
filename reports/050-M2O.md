packet:            M2O — Fixture isolation + accounting fail-open
session:           codex / 2026-08-03 / c81e
status:            DONE
what exists now:   Every deterministic Harness scenario installs one shared
                   reachability wall: server-verified ` REGRESSION` identity,
                   product-port refusal, root marker redirect, and the
                   viewport-covering FIXTURE overlay. The foreground launcher
                   rejects 8765, current fixture SOPs use it, and the M2O
                   browser proof owns and removes its temporary Chrome profile
                   and fixture process.

                   The thread rail offers a human-triggered exact-title cleanup
                   only for known legacy fixture catalog entries. A failed or
                   incomplete spend batch now leaves the completed answer
                   intact, queues stable-ID `basis=estimated` receipts in an
                   atomic mode-0600 owner-local spool (memory fallback), replays
                   oldest-first, and exposes pending/degraded accounting drift
                   in both public Rack Vitals scopes. Harness commits 2d32540
                   (receipt queue, daemon integration, Vitals contract, Decision
                   030) and 5ab237f (fixture wall, cleanup, built Rack, tests,
                   rendered evidence) contain the product result.
deviations:        Harness Decision 030 (P3, P4.1) and Garden A-038 complete
                   fixture identity, cleanup, queue replay, and Rack accounting
                   details left silent by B.6 rules 10-11. No vision deviation.
evidence:          Focused M2O verification passed 71 tests, including a dead
                   ledger turn that returns `answer`, durable 0600 estimated
                   replay, degraded memory fallback, socket-port refusal under
                   a misleading Host header, launcher rejection of 8765, and
                   every scenario's shared wall. Full Harness ground passed
                   597 tests with three live-contract tests intentionally
                   deselected; full Spine ground passed 208 tests; Ruff passed
                   in both repos. Web ESLint, six unit tests, TypeScript, and
                   Vite production build passed.

                   `verification/m2o/rendered.json` is PASS. Its disposable
                   browser pass proves a server-verified overlay covering
                   1440x900 and 390x844, rejects a query-string-only marker,
                   renders `Receipt drift · 2 lines pending`, removes exactly
                   two seeded fixture entries, preserves `Owner planning
                   notes`, and records three screenshots in that directory.
notes to the next agent:
                   M2N remains blocked only on owner decision F015; do not
                   implement its restore semantics. Run a fresh boot sequence;
                   M2R is now the first dependency-ready TODO row by board
                   order. The M2O visual fixture deliberately omits a chat
                   runner, so its JSON records the expected fixture-only 503s;
                   the proof surface is fixture identity, catalog cleanup, and
                   Vitals, not a model-turn fixture. Receipt idempotency still
                   relies on A-027's stable event_uid contract in Spine.
