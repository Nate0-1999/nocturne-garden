packet:            H2 — spine_client + contract tests
session:           codex / 2026-07-20 / db84
status:            DONE
what exists now:   Harness owns an async client for all seven C.4 operations,
                   with exact status/media/body correlation, strict standard
                   JSON, safe base-URL joining, owned transport lifecycle, and
                   separate typed transport, RFC7807, create-conflict, and
                   PATCH-conflict failures. Current origin_path and paging /
                   search / prepare bounds are mirrored. An always-on CI job
                   builds pinned Spine commit 9c51c992, migrates disposable
                   pgvector, injects closed-set deterministic embeddings only
                   at test composition, and drives S1–S2 through real HTTP.
deviations:        Garden A-014 completes model_context_tokens > 0 before
                   embedding/write. Harness DECISIONS 005 (P1.1) records the
                   literal transport and disposable live-contract boundary.
                   No FLAG and no Spine production change.
evidence:          Harness verification/h2/README.md; 87 non-contract tests,
                   51 focused client/model tests, 2 live contract tests after
                   Alembic 0001/0002, and 158 Spine tests. Ruff lint/format, uv
                   lock, scope fence, shell/workflow/Compose validation, web
                   lint/build, cleanup inventory, and three independent final
                   audits are green with no blocker.
notes to the next agent:  Harness remote head is
                   63c24bce8492cd07cba7672a083c6476c6dfbdf8. Use SpineClient
                   as an async context manager. Create 200/201 and create/PATCH
                   JSON 409s are deliberately status-correlated; RFC7807's
                   standard members remain optional, and retries/redirects are
                   deliberately absent. The CI pin is the current v1.12 Spine
                   freeze; update it consciously when Spine behavior changes.
                   The contract runtime is isolated/no-cloud but its production
                   base image, dependency ranges, and pgvector tag are not
                   digest-locked. SPEC v1.12 arrived during H2 but changed C.7,
                   not C.4; H7 owns that follow-up. H3 is next by board order,
                   while D1 remains human-owned and IN_PROGRESS. Stop here.
