packet:            M2K — Memory Graph + console + contribution bars
session:           codex / 2026-08-03 / 7ac4
status:            DONE
what exists now:   Spine owns repeatable-read Memory Graph snapshots over the
                   non-candidate corpus, including complete MemoryUnit bodies,
                   revision trails, exact similarity edges, durable lineage,
                   edit-trail evidence, and explicit CURRENT membership. The
                   scorer console reads immutable injection history and emits
                   six weighted contributions plus an exact bias residual that
                   conserves every recorded score.

                   Human scorer control is durable authority, not browser
                   state: eleven law-bound descriptors create a new active
                   scorer version under one advisory transaction lock and
                   append a scorer_activation record. Learner proposals keep
                   their distinct provenance and activation operation. Thread-
                   scoped Vitals reads authoritative spend_event receipts;
                   GLOBAL keeps the existing aggregate view.

                   Harness ships two sandboxed first-party Rack instruments.
                   Memory Graph uses deterministic SVG node/edge encodings,
                   GLOBAL/CURRENT scope, inspection, and the public memory
                   selection seam into the existing CAS editor. Injection
                   Console exposes live candidate histories, threshold/rank/
                   placement, exact contribution bars, honest Not scored yet,
                   accuracy, proposals, and an explicit Enact version action.
                   Gate and Memory Palace cards consume the same recorded
                   contribution truth. Vitals now adopts the same persisted
                   GLOBAL/CURRENT scope grammar.
deviations:        Garden A-035 enacts wire shapes omitted by PLAN: graph
                   membership and edge threshold, revision/edge encodings,
                   scorer descriptor bounds and weight conservation, exact
                   contribution arithmetic, append-only activation provenance,
                   and the authoritative source for CURRENT spend. WHAT-IF and
                   AUDITION remain deliberately absent for M2P. M3 graph bulk
                   actions, 3D rendering, cross-Palace views, and a second
                   memory editor were not built.

                   Experiential testing exposed two real integration defects:
                   the existing layout controls intercepted the new desktop
                   launchers, and the initial mobile breakpoint hid both
                   instruments. The layout-control hit area and mobile launch
                   path were repaired before final evidence. Node editing
                   remains in Memory Palace rather than duplicating CAS logic.
evidence:          Spine 8ffff28 and Harness 1249fe2 are pushed to origin/main.
                   Spine's locked Colima suite passes 203 tests and Ruff.
                   Harness passes 586 non-live tests and Ruff. Its three
                   external live-contract tests were intentionally excluded
                   because this session had no SPINE_URL/SPINE_TOKEN; their
                   initial fail-closed setup errors did not execute product
                   code. Web ESLint, six unit tests, TypeScript, and Vite build
                   all pass with committed dist.

                   The visibly bannered deterministic browser proof passed at
                   1440x900 and 390x844: two graph nodes and their similarity
                   edge rendered with scope truth; the console rendered all
                   eleven controls, seven exact contribution lanes, honest Not
                   scored yet, a conserved 1.00 weight sum, and successfully
                   enacted a new version. Both desktop surfaces and the mobile
                   console reported no horizontal overflow. Screenshots,
                   rendered trace, fixture, and SOP are committed under
                   verification/m2k/.
notes to the next agent:
                   M2L is next by deterministic board order; do not claim it in
                   this session. Preserve A-035's separation: M2K control
                   creates/activates versions, while M2P later owns what-if and
                   audition. D3 remains blocked solely on the owner-created
                   PyPI trusted publishers.
