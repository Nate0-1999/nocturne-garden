packet:            I1 — FIXER for J1/J2 evidence repair
session:           codex / 2026-07-31 / 7f26
status:            BLOCKED
what exists now:   The inherited product ground remains green and unchanged.
                   The verdict's J2 gap has a lawful evidence-only path: an
                   ordinary fresh-word chat turn can explicitly request one
                   force=false save_memory call, then correlate its C.7 tool
                   event to the single Spine 409/similar response and SQL.
                   J1 cannot be closed under current law: F012 records the
                   conflict between its same-thread model switch, A-020's
                   thread-stable resolution, and the M2 selector boundary.
deviations:        none — no product, SPEC, Amendment, ADR, or evidence
                   artifact was changed; F012 stops the line rather than
                   fabricating a switch or pulling M2 behavior into M1.
evidence:          Claim commit 7c336ee. Harness mandatory suite:
                   356 passed, 2 deselected. Spine mandatory suite:
                   160 passed. Read-only implementation audit confirmed the
                   UI model surface is display-only, prompt.submit carries no
                   consumed model override, and RunLoop rejects a changed
                   resolution after the first run.
notes to the next agent:
                   Do not attempt two daemon starts and call that a
                   same-thread switch. Do not add a selector without the
                   owner's milestone/contract decision. Once F012 is
                   resolved, rerun only J1/J2: retain the hello run's exact
                   C.7 envelopes; for J2 use a distinct label and fresh-word
                   body, request exactly one save_memory call with
                   force=false, and correlate its tool_call_id/run_id to one
                   Spine 409 or similar[] result. Then return I1 to DONE and
                   J to TODO for a fresh independent judge. No undefined
                   glossary term was introduced.
