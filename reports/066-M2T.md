packet:            M2T — Owner-cloud credential alignment + M2 deploy
session:           codex / 2026-08-06 / c810
status:            RETURNED_TODO
what exists now:   Harness b617485 implements D.2 098's infrastructure/data-
                   plane scope boundary and mutation-last ordering. A
                   verification-only apply mints no backup receipt; every
                   infrastructure CREATE/UPDATE still does. Credential
                   alignment now materializes source and completes/proves the
                   image before its first service-affecting mutation.

                   The real production dry-run is exact at every
                   infrastructure stage and asks only for remote verification.
                   Production itself is unchanged: revision 00005-t64 still
                   serves 100% default traffic from image 0.1.0, and the
                   verification label remains absent.

                   M2T is not done. The execution environment rejected the
                   live command before creating its process because it
                   requires fresh explicit owner authorization for the
                   isolated memory round trip and Cloud Run verification-label
                   write. No endpoint request, receipt, backup, label, or
                   other production mutation occurred.
deviations:        none. F019 remains resolved and D.2 098 remains controlling
                   law. The execution-authorization boundary is not a SPEC
                   change or a new flag.
evidence:          Fresh inherited ground: Spine 222 passed and Ruff clean;
                   Harness 650 passed / 3 contract tests deselected and Ruff
                   clean; web ESLint, seven unit tests, TypeScript, and Vite
                   production build passed.

                   Post-change Harness proof: 654 passed / 3 contract tests
                   deselected; Ruff clean; git diff check clean. Regressions
                   cover verification-only no-receipt, secret-only receipt,
                   source-before-receipt alignment, image-before-alignment,
                   and non-converging-image stop-before-reset behavior.

                   The real `nocturne deploy --dry-run` exited 0 with stages
                   01-18 and 20 NOOP and only stage 19 remote_verification
                   UPDATE. Four private receipts remain; newest
                   01KZD3K0CVGV29JMG7QAF9TPZ5. The latest on-demand Cloud SQL
                   backup remains 1786072630287. Cloud Run pre-state is
                   recorded without secrets in Harness
                   verification/m2t/verification-only-readiness.md.
notes to the next agent:
                   Boot and pull all three repos. M2T is first eligible; do not
                   claim M2U or the HUMAN M2X packet.

                   Before the live command, obtain fresh explicit owner
                   authorization that names the isolated production memory
                   create/dedup/injection/cleanup round trip, Vitals read, and
                   Cloud Run `nocturne-verified` label write. Clarify that all
                   infrastructure stages are NOOP and no receipt is minted.
                   Do not retry through another tool or indirect path if the
                   execution boundary has not been cleared.

                   Then run `uv run --locked nocturne deploy` once. On success,
                   require a follow-up dry-run with all 20 stages NOOP; prove
                   the four-receipt set and latest on-demand backup are
                   unchanged; verify label
                   6ad9613a455798d6d92e5f5f390ab4ba, revision/traffic, typed
                   request statuses, cleanup, and Vitals without recording
                   secrets. Update the Harness readiness evidence into closure,
                   push it, write the next Garden report, and mark M2T DONE.
