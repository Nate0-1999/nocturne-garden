packet:            P0 — Agent Zero v1.5 refresh
session:           codex / 2026-07-17 / 70ed
status:            DONE
what exists now:   Spine commit 1732d26 freezes SPEC v1.5, adds the exact C.2
                   active-label partial index, and publishes strict v1.5 C.4
                   request/query/success/conflict schemas while all seven
                   routes remain named RFC7807 501 stubs. Harness commit
                   d6a7394 freezes the same law and mirrors every C.4 shape in
                   typed, transport-free stubs. PLAN §6 ground rules are exact
                   in both repos; no M1 business behavior was built.
deviations:        Spine DECISIONS 003 [P1.1] publishes the now-exact success
                   schemas in OpenAPI without behavior. Harness DECISIONS 003
                   [P1.1] records v1.5's dictated supersession of its historical
                   opaque models. No Garden amendment or contract deviation.
evidence:          spine/verification/bootstrap/README.md: 17 tests, Ruff,
                   deterministic OpenAPI, fresh Compose migration 0001 head,
                   exact partial index/v0 seed, health/auth, seven live named
                   501s. harness/verification/bootstrap/README.md: 27 pass,
                   1 intentional H2 skip, Ruff, npm lint/build, zero npm audit
                   findings, live shell + exact C.7 echo, and fresh 390x844
                   no-overflow/no-console-error browser evidence. Both scope
                   fences and CI YAML checks passed; final audit found no
                   forbidden implementation or cross-repo schema drift.
notes to the next agent:  Dispatch stops here: one packet per session. P0 is
                   DONE; row order makes S1 next (SPEC C.2 + ADR-004), while H1
                   is also dependency-ready afterward. Migration 0001 changed
                   before any implementation packet was DONE: use a fresh DB
                   volume, because an old v1.4 volume already stamped 0001 will
                   not replay the new index. On this Colima workstation,
                   testcontainers needs
                   `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock`.
                   If macOS hides an editable-install `.pth`, rebuild the
                   generated `.venv` before diagnosing source.
                   No repository has a remote, so commits a255ece, 1732d26,
                   d6a7394, and this Garden handoff could not be pushed. This
                   limitation is recorded, not represented as a completed push.
