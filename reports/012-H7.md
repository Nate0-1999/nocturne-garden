packet:            H7 — Envelope v1.12 & loop controls
session:           codex / 2026-07-20 / 9d2f
status:            DONE
what exists now:   Harness validates the enacted A-016 payloads and open
                   extension boundary, then owns daemon-lifetime thread
                   transcripts, opaque provider history, one active run per
                   thread, FIFO prompts, cumulative usage, snapshots, and
                   exactly-once cancellation/terminal ordering. A bounded,
                   ordered subscriber/outbox path survives reconnect and slow
                   consumers. The pydantic-ai adapter streams typed deltas,
                   classifies every terminal path, repairs interrupted tool
                   calls, and is reachable through the real `harness dev`
                   composition with trusted local identity and one Spine client.
deviations:        Garden A-016 completes C.7 v1.12's executable payload,
                   correlation, queue, cancellation, snapshot, and extension
                   rules. Harness DECISIONS 009 (P3) and 010 (P3/P1.4) record
                   the authoritative process loop, bounded delivery, adapter,
                   cancellation repair, and trusted dev composition. No FLAG
                   and no Spine production change.
evidence:          Harness verification/h7/README.md; 218 non-contract tests,
                   103 focused H7 cases, 41 loop/daemon cases under asyncio
                   debug with RuntimeWarnings fatal, and 2 live migrated Spine
                   contract cases. The sibling Spine suite passes 158 tests;
                   Ruff lint/format, uv lock, M1 scope, web lint/build, diff
                   checks, a real Uvicorn/WebSocket probe, and three independent
                   adversarial audits are green.
notes to the next agent:  Harness remote head is
                   19605845ea722b3aec8be78eb66fb991cde92e51. Most H7 files
                   appear in its parent 274c540 because the concurrent v1.15
                   human-gate commit consumed the already-staged H7 tree; the
                   H7 commit itself closes the final direct-delivery worker
                   leak. Do not rewrite that published history. C.7 behavior
                   did not change in v1.15. H4 is now dependency-ready and,
                   because it touches user-facing surface, must also read
                   garden/NATES_VISION.md under the new Boot STEP 5 rule.
                   Consume the exact envelope models rather than duplicating
                   their wire shapes. `harness dev` uses `create_dev_app`;
                   thread IDs become UUIDs only at the trusted memory context
                   boundary. Snapshot null members are required. H5 may extend
                   gate payload extras through the existing emitter, but must
                   not bypass RunLoop ordering or add M3 control behavior.
                   D1 remains human-owned and IN_PROGRESS. Stop here under the
                   one-packet rule.
