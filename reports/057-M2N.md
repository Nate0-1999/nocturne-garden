packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / 8c7e
status:            RETURNED_TODO
what exists now:   Harness commit c7c752b implements v2.49's informed local
                   restore. Config version 3 owns one constrained active-volume
                   pointer; version 2 preserves the existing Compose volume
                   during atomic upgrade. `nocturne restore BACKUP_ID` refuses
                   while the owner services can write, reverifies the A-042
                   generation, restores and migrates an isolated fresh volume,
                   and computes its comparison against the live Palace.

                   The rollback manifest names lost memories, reverted edits,
                   undone pins, and current/restored/delta counts for six
                   durable event tables. Only exact `RESTORE BACKUP_ID`
                   confirmation switches the named volume. A failed candidate
                   leaves live state untouched; a failed mechanical switch
                   restores the former pointer and container. Success retains
                   the former volume and records both volumes in a private
                   rollback receipt.

                   M2N remains incomplete only at the owner-cloud migration
                   seam: a durable Cloud SQL pre-migration backup receipt must
                   exist before `GcloudDeployBackend._apply_migrations`.
deviations:        Garden A-045 [P1.3, P4] and Harness Decision 037 define the
                   durable volume pointer, exact rollback inventory, typed
                   confirmation, automatic pointer rollback, and private
                   former-volume receipt. This executes the owner's F015/v2.49
                   decision without authorizing in-place or cloud restore. No
                   vision deviation and no new glossary term.
evidence:          Fresh boot ground: Harness 618 passed / 3 live contracts
                   deselected; Spine 221 passed through Colima; web ESLint, 7
                   Node tests, TypeScript, and Vite build passed.

                   Post-change: Harness 626 passed / 3 live contracts
                   deselected; Spine 221 passed; web ESLint, 7 Node tests,
                   TypeScript, and Vite build passed. Full Harness Ruff lint
                   and all changed-file Ruff formatting passed. Motivation
                   check passed at 348 discovered / 308 grandfathered and law
                   coverage was regenerated.

                   `verification/m2n/restore-smoke.md` records the real pinned-
                   image proof: custom backup, isolated restore, packaged
                   migration, named 1-loss/2-edit/1-pin manifest, volume switch,
                   restored contents, private receipt, former-volume retention,
                   and complete Docker cleanup. Its first execution found a
                   Docker env-file quoting mismatch before any switch; the
                   candidate cleanup ran, a regression was added, and both
                   corrected executions passed. Final inspection found no
                   labeled restore containers or volumes.
notes to the next agent:
                   Run a fresh boot; M2N remains first eligible by board order.
                   Local restore is now complete. Do not reopen its data-loss
                   boundary unless fresh evidence finds a defect.

                   Finish the one remaining duty at
                   `GcloudDeployBackend._apply_migrations`: create the specified
                   Cloud SQL pre-migration backup and persist a safe receipt
                   before the migration call. Reconcile against existing D1/D3
                   authority and Google operation semantics; do not imply that
                   cloud restore is automated, because v2.49 keeps it a human
                   Cloud SQL operation. After full gates and redacted evidence,
                   M2N should be eligible for DONE. Do not begin M2Q or M2P.
