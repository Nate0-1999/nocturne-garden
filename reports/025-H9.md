packet:            H9 — Model policy routing A-020/021
session:           codex / 2026-07-30 / 86af
status:            DONE
what exists now:   Harness commit b7bcd48 implements the exact five-value
                   A-021 chat policy, strict-under-24h broker catalog cache,
                   deterministic Decimal policy math, one stable resolution
                   per daemon-lifetime thread, and static fail-open. The same
                   resolved route/context now governs model display, memory
                   prepare, ordinary chat, and /remember. Every OpenRouter
                   request carries thread session stickiness; non-pinned
                   policies also request price-sorted providers. Existing run
                   usage now retains cache-read and cache-write tokens.
deviations:        Harness DECISIONS 017 [P4, P4.2]. Garden A-025 completes
                   zero-priced elbow behavior as degenerate/static fail-open.
                   Garden A-026 binds benchmark canonical slugs to one
                   executable model-list ID and its context without silently
                   opting into an explicit broker variant. H8's human model
                   selector remains M2 scope; no selector was built.
evidence:          Harness: 350 passed, 2 live-contract tests deselected.
                   Spine: 160 passed. Ruff check and format check, uv lock
                   check, git diff check, web TypeScript/Vite production
                   build, and ESLint all passed. verification/h9/ contains a
                   repeatable credential-safe live broker probe and the
                   2026-07-30 receipt: 101 complete benchmark rows, 330
                   unambiguous runnable routes, and exact successful
                   max/slope:0.05/floor:52 route+context joins. Independent
                   law, code, and adversarial A-026 reviews returned PASS
                   after four caught regressions were fixed.
notes to the next agent:  I1 is now dependency-ready; H5's owner hold still
                   gates J only. MODEL_POLICY_CHAT remains intentionally unset
                   in the ignored local .env, so current user behavior stays
                   pinned to CHAT_MODEL until explicitly enabled. The live
                   frontier currently contains zero-priced
                   cohere/north-mini-code-20260617, so `elbow` lawfully fails
                   open to the static pair under A-025 today; do not claim an
                   economic elbow selection in I1 evidence unless the live
                   table changes. `slope:0.05` resolved the standard
                   minimax/minimax-m3 route at 1,048,576 context tokens in the
                   recorded probe. Provider model objects are lazily cached
                   but still lack an explicit public close ownership path
                   (pre-existing for the default client, broader under H9);
                   treat that as lifecycle debt, not permission for a private
                   Pydantic-client shutdown hack. No glossary entry proposed.
