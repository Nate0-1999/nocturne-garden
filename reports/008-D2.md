packet:            D2 — Billing circuit breaker
session:           codex / 2026-07-19 / b61b
status:            DONE
what exists now:   Spine has an isolated Python 3.12 Cloud Run function whose
                   exact-decimal decision core detaches only the fixed
                   n8-memory-palace project at costAmount >= budgetAmount.
                   Fixture tests cover below/equal/above, malformed, foreign,
                   duplicate, adapter, IAM, budget, Eventarc, Pub/Sub, and
                   recovery boundaries. The default-inert human runbook and
                   deploy script validate topology and least privilege before
                   arming, then provide a correlated drill and fail-closed
                   teardown/reattach procedure. Nothing was deployed.
deviations:        Spine DECISIONS 013 (P4). During development, a removed
                   local test invoked deploy.sh --dry-run against a fake
                   gcloud sentinel. It exited before lookup or mutation but
                   still crossed D2's literal agent boundary; the evidence
                   records this procedural deviation.
evidence:          Spine verification/d2/README.md; 158 tests passed, including
                   76 isolated no-cloud D2 tests. Ruff lint/format, uv lock,
                   scope fence, Compose/OpenAPI/zero-501 checks, shell syntax,
                   exact-pinned runtime adapter smoke, production Docker build,
                   and three independent blocker audits passed. Harness: 27
                   passed with the intentional H2 live-contract skip; Ruff,
                   format, uv lock, scope fence, web lint/build passed.
notes to the next agent:  Spine remote head is
                   9176a7755bffa2653524997c6186c1f197e1f875. D2 is source and
                   runbook only: a human must audit inherited/custom IAM,
                   trusted admins, the exact existing billing-account-owned
                   USD 100 monthly budget, actual spend, and off-project backup
                   before deciding whether to run infra/billing-breaker/README.md.
                   Budget notifications are delayed and this is not a hard
                   real-time cap; successful detach can remove project
                   resources. D1 remains human-owned and IN_PROGRESS. Stop here
                   per the one-packet relay rule; H1 is the next eligible
                   agent packet by board order.
