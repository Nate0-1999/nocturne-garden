packet:            M2A — Spend ledger core
session:           codex / 2026-08-01 / a4d2
status:            DONE
what exists now:   Spine owns an append-only `spend_event` receipt ledger at
                   Alembic head 0003, protected by a database mutation trigger
                   and an authenticated, atomic, replay-safe
                   `POST /v1/spend/events`. Five minute-refreshed canonical
                   materialized views provide the ADR-024 spend, thread, run,
                   memory, and cache-efficiency grains without becoming
                   authority. Production embedding calls synchronously record
                   `llm.embedding` usage before returning vectors. Harness
                   converts every new broker model response into honest,
                   nonzero price-class lines and persists the complete batch
                   before terminalizing the turn; receipt failure is visible
                   and terminal. The OpenAPI and typed Harness client match the
                   enacted wire contract.
deviations:        A-027 completes ADR-024's missing cross-process ingest,
                   idempotency, price-class, aggregate-allocation, and ref
                   fallback law. Spine Decisions 020–021 and Harness Decisions
                   022–023 record the implementation choices and retire the
                   closed-M1 regex fence that otherwise rejected lawful M2
                   work. Two inherited packaging blights were repaired: the
                   Spine Docker builder now carries its package-declared
                   README, and the Harness contract stack uses Spine's existing
                   packaged migration entrypoint rather than an Alembic source
                   path. No UI, spend wall, reconciliation job, or new cloud
                   resource was built.
evidence:          Spine 371b698425b607d7dac72213367a2f78d00da145;
                   Harness 4a59046e1f814db1b74906812145d37843982629;
                   Garden 6622d1b enacts A-027. Spine final suite: 174 passed;
                   Ruff green. Harness final suite: 531 passed / 3 contract
                   tests deselected; Ruff and format green. The live Docker
                   contract built the Spine wheel, migrated a real pgvector
                   Postgres instance, and passed all 3 tests, including receipt
                   insert, identical replay, and differing replay conflict.
                   `spine/verification/m2a/README.md` and
                   `harness/verification/m2a/README.md` map those checks to the
                   delivered seams. Both product heads equal `origin/main` at
                   handoff.
notes to the next agent:
                   M2A did not grant deployment authority. The committed code
                   and migration head are 0003, but the existing Cloud Run
                   service remains on its earlier deployed code/schema until a
                   separately authorized deploy applies them. M2C may build
                   against the five canonical views; it must not infer that the
                   live service already exposes them. M2M owns broker
                   self-audit, and the owner-gated GCP billing export remains
                   deferred under ADR-024. D3 remains independently blocked on
                   F014. The next deterministic claimable packet is M2B; this
                   session does not claim it.
