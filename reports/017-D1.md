packet:            D1 — GCP deploy & remote verify
session:           codex / 2026-07-20 / b82e
status:            DONE
what exists now:   Cloud SQL, regional secrets, dedicated runtime IAM, immutable
                   Artifact Registry delivery, and one Gen2 Cloud Run Spine are
                   live in n8-memory-palace/us-central1. Revision
                   n8-memory-palace-spine-00003-pjh serves 100% of traffic at
                   https://n8-memory-palace-spine-713925718873.us-central1.run.app.
                   Real OpenRouter create → hard duplicate → prepare → commit
                   and a typed Harness list smoke both passed; harness/.env now
                   points at the service and remains mode 0600.
deviations:        Spine Decision 016 (P4) adds authenticated, OpenAPI-hidden
                   /health as the Cloud Run alias for the specified /healthz.
                   Google reserves some URL paths ending in z and intercepted
                   /healthz before Spine; /healthz remains unchanged for local
                   Compose acceptance. No spec amendment was required.
                   The diagnosis also disabled the Cloud Run Invoker IAM check
                   and left the service-scoped allUsers Invoker binding, while
                   the static Spine bearer still protects every app route. A
                   diagnostic d1v traffic tag remains pinned to revision 00002;
                   it carries no default traffic and creates no second service.
evidence:          spine/verification/d1/README.md and cloud-state.json at Spine
                   commit cf1c6105d366c12c76bde036397657e0eaa987c5. Deployed
                   source/image SHA e0cf50d50283cd2c4f800272b832b8166e299cab;
                   index digest sha256:dd06579e7447c1110ecb7c9be190d293ed79bb319d1b99b1299ee736b648cad3;
                   deployed amd64 digest sha256:975c1e68386c801cedcc3ea20e06009e54b7e9859b7d1551c4835821baaff113.
                   Final suites: Spine 160 passed in 5.19s; Harness 220 passed,
                   2 deselected in 1.20s. Ruff, formatting, locks, OpenAPI,
                   whitespace, evidence JSON, app auth, cloud config/IAM, and
                   revision request logs all passed inspection.
notes to the next agent:  H5 is now the deterministic next packet. Treat D1 as
                   inherited deployed ground: do not reprovision it. Cloud
                   probes must use authenticated /health because Cloud Run's
                   front end returns its own HTML 404 for /healthz; this is a
                   documented platform reservation, not a dead service. Use the
                   ignored harness/.env for SPINE_URL/SPINE_TOKEN without
                   printing values. OpenRouter is the default chat+embedding
                   broker; the OpenAI env line is a legacy direct-provider
                   override. GCS is M4, not missing D1 work. Pre-existing
                   untracked .DS_Store files remain untouched in both product
                   repos.
