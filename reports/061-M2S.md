packet:            M2S — Rung-2 first-class startup
session:           codex / 2026-08-05 / c805
status:            DONE
what exists now:   Harness commit 20414bf makes the Palace rung explicit in
                   the existing private Nocturne config. `nocturne init
                   --remote <spine-url>` uses the environment OpenRouter key
                   or prompts for it, privately prompts for the Palace bearer,
                   normalizes the service origin, and writes remote mode.

                   `nocturne up` now branches only at owned-service startup:
                   local mode retains Docker, backup, migration, Spine, and
                   daemon; remote mode authenticates the owner-operated Spine
                   and starts only the same packaged daemon before opening the
                   Rack. `nocturne doctor` reports remote health, journal, and
                   disk, and plainly skips local database/backup checks.

                   Existing version-3 configs upgrade atomically to explicit
                   local mode. README gives checkout/.env users the two-command
                   migration. The Rack's false `local direct` label is now the
                   mode-neutral `Linked`, with an explicit control gap found
                   and corrected during the visual pass.
deviations:        None. Harness Decision 040 records the one-config rung seam
                   under P4. No Garden term or build reasoning reaches the
                   customer-facing command or Rack text.
evidence:          Fresh boot ground: Spine 221 passed. Final Harness standard
                   ground: 638 passed, three live SPINE_URL contracts
                   intentionally deselected. Web ESLint passed; all seven Node
                   unit tests passed; TypeScript and production Vite build
                   passed. A fresh pair of lockstep wheels passed the
                   installed-wheel smoke outside the source checkout.

                   `verification/m2s/SOP.md` records the live remote walkthrough
                   against the owner Spine from an isolated NOCTURNE_HOME. The
                   authenticated doctor passed after a real scale-to-zero wake;
                   the packaged Rack returned 200, rendered `Linked` and `Link
                   live`, loaded remote memories, produced no browser warnings
                   or errors, and measured an 8px gap at the repaired header
                   seam. Ctrl-C shut the single daemon down cleanly.
notes to the next agent:
                   Fresh boot dispatches SCOUT for M2X; M2X is HUMAN and must
                   never be agent-claimed. Derive its missing checklist before
                   executing the agent-safe tiers.

                   Do not present the owner with the gate yet without handling
                   this observed environment truth: the deployed owner Spine
                   answers health and current-memory calls, but the assembled
                   M2 Harness receives HTTP 503 for current Vitals and scorer
                   console queries. This is cloud deployment-version skew, not
                   an M2S startup failure. The M2X scout must resolve it within
                   its authority or classify it honestly for the owner. D3
                   remains independently owner-deferred on F014; do not publish
                   or substitute long-lived credentials.
