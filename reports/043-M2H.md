packet:            M2H — Extraction + queue + thread-end card
session:           codex / 2026-08-03 / d4e2
status:            DONE
what exists now:   Harness extracts the durable M2D transcript on explicit
                   archive or the configurable idle fallback, returning a
                   working summary, open loops, and no more than five atomic
                   keyworded candidates. Before queue birth, a separate
                   structured verdict pass sees machine-fetched active corpus
                   neighbors and proposes new, merge, supersede, or contradict.
                   Archive is idempotent at the exact durable transcript tail.

                   Spine persists candidate heads, queue cards, append-only
                   decisions, and verdict edges. Candidate dedup covers ACTIVE
                   and CANDIDATE heads while ordinary list/search/injection
                   keep candidates invisible. Approval activates the candidate
                   and deterministically enacts the proposed verdict; rejection
                   revision-tombstones it. Merge and supersede revision-
                   tombstone their targets, contradiction preserves both heads,
                   and passive contradiction is mechanically rejected. Queue
                   decisions are identical-retry idempotent and conflicting-
                   retry 409. Palace Vitals now measures candidate, edge, and
                   queue depth instead of publishing placeholders.

                   Archive opens a law-bound thread-end rack module with the
                   final assistant post above the consent cards. CURRENT is the
                   default scope; GLOBAL|CURRENT is persisted only in rack
                   presentation state. Each candidate has explicit approve and
                   deny controls. A bounded internal list keeps the resolve
                   control present without scrolling unseen rows through the
                   viewport. Literal full-row IntersectionObserver visibility
                   is the only passive signal; collapsed groups resolve none,
                   unseen rows remain pending, and contradictions always need
                   an explicit human tap.
deviations:        Garden A-032 completes the queue schema, API, trigger, and
                   minimal verdict mechanics that ADR-021 deliberately leaves
                   between M2H and the later M3 curator tools. No M3 curator
                   agent, health report, generic tool SDK, graph retrieval,
                   timer-based consent, or owner notification was added.

                   Browser rule 8 found and repaired two integration defects
                   before close. First, the new HTTP routes had been composed
                   after the catch-all static mount, making Archive return 405;
                   `create_app` now accepts an explicit pre-static route hook
                   and has a regression test. Second, the resolve control sat
                   after the list, so reaching it scrolled every row through
                   visibility. The candidate list now owns the scrolling while
                   the consent footer remains fixed. These were product defects,
                   not fixture exceptions.
evidence:          Spine e73d420 and Harness c452499 are pushed to origin/main.
                   Final locked suites: Spine 198 passed; Harness 580 passed
                   with the three separately deployed live-contract tests
                   deselected; both Ruff suites pass. Web ESLint passes, six
                   unit tests pass, three rack-layout tests pass, and the
                   TypeScript/Vite production build passes with committed dist.
                   Spine migration 0006 and authoritative metadata/OpenAPI tests
                   cover candidate status, queue tables, decisions, edges,
                   foreign keys, checks, defaults, and route contracts.

                   The visibly bannered deterministic M2H fixture exercises the
                   real built rack and daemon boundary. In the in-app browser,
                   Archive returned 200 and opened the independent thread-end
                   module with five typed cards and the exact final post. A
                   collapsed five-card group left the resolve control disabled
                   and the fixture trace at zero decisions. An explicit
                   contradiction produced human/explicit provenance. At
                   390x500 after the scroll-boundary repair, resolve-visible
                   passively approved exactly three fully seen non-
                   contradictions; one unseen card and the contradiction stayed
                   pending (trace: three passive decisions, pending=2). Closing
                   and reopening the module preserved a chosen GLOBAL scope.
notes to the next agent:
                   M2I is next by deterministic board order; do not claim it in
                   this session. Its seed ingestion should feed the same durable
                   approval queue rather than inventing a second staging path.
                   Preserve the separation between extracted candidates and
                   active search/injection, and do not treat queue decisions as
                   M2F training dispositions without a later explicit law.
                   M2J/M2K remain SPA-excluded from M2H only while M2H was active;
                   D3 remains blocked solely on the owner-created PyPI trusted
                   publishers.
