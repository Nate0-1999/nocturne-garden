packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / f04b
status:            RETURNED_TODO
what exists now:   Harness commit d401cf4 adds `nocturne backup` as the first
                   lifecycle authority. It streams a PostgreSQL custom archive
                   from the pinned local Compose database, verifies it with the
                   matching pg_restore, publishes a private ULID generation and
                   integrity receipt atomically, and retains only the configured
                   number of fully recognized generations.

                   Local `nocturne up` now creates that same receipt before its
                   packaged migration attempt; backup failure stops migration.
                   Receipt and archive paths are documented in the owner README.

                   M2N remains incomplete. Informed side-by-side restore, doctor,
                   owner-cloud pre-migration backup receipts, resource Vitals,
                   and the daemon soak bound remain unbuilt.
deviations:        Garden A-042 [P1.3, P4] and Harness Decision 034 define one
                   verified local backup artifact without changing v2.49 restore
                   semantics or the owner-cloud HUMAN recovery boundary. No
                   vision deviation and no new glossary term.
evidence:          Harness: 611 passed, 3 live contract tests deselected; Spine:
                   220 passed through Colima/Testcontainers. Full Ruff lint
                   passed in both repos; all changed Harness files pass Ruff
                   format. Web ESLint, 6 Node unit tests, TypeScript, and Vite
                   build passed. Motivation check: 334 discovered / 309
                   grandfathered; law coverage regenerated with A-042 defenders.
                   `verification/m2n/backup-smoke.md` records an isolated real-
                   container smoke: pg_dump + pg_restore verification, receipt,
                   modes, and complete temporary-resource cleanup. Full-tree
                   Harness format-only check still reports eight unrelated
                   pre-existing files; none was changed or mass-formatted.
notes to the next agent:
                   Run a fresh boot; M2N remains first eligible by board order.
                   Reuse `harness.lifecycle.create_local_backup` and its receipt
                   validator rather than adding another archive authority.
                   Restore must remain literal v2.49: fresh side-by-side volume,
                   restore and verify there, compute the named rollback manifest,
                   explicit confirmation, then switch while retaining the former
                   volume. A failed restore never touches live state.

                   The owner-cloud seam still begins at
                   `GcloudDeployBackend._apply_migrations`; add a Cloud SQL backup
                   receipt before that call without pretending cloud restore is
                   automated. Doctor should validate the same receipt/archive and
                   add disk, journal, and database size checks. Finish resource
                   Vitals and the stated soak bound before closing M2N. Do not
                   begin M2Q or M2P.
