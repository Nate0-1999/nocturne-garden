packet:            M2C — Palace Vitals + spend strip
session:           codex / 2026-08-02 / c2c0
status:            DONE
what exists now:   Spine serves A-028's bearer-protected, no-query
                   `GET /v1/vitals` from one repeatable-read snapshot over
                   ordinary `v_spend_rate` cadence and current memory heads.
                   Exact Decimal total/purpose/model lanes conserve priced
                   dollars, receipts, and unpriced lines; A-029 keeps null,
                   literal `unreported`, and leading-tilde model identities
                   collision-free. Gauges measure only created rate and
                   current active/pinned counts; every unavailable lifecycle,
                   Palace, and queue signal is typed honestly.
                   Harness mirrors and validates that contract, reads it with
                   the daemon's existing Spine bearer, and exposes only the
                   typed result across ADR-023's public rack query bridge. A
                   compiled first-party Vitals resident remains sealed by
                   `connect-src 'none'`; it renders exact dollar lanes,
                   shared hover/touch/keyboard scrub, selection focus,
                   collapse/reallocation, sparse-minute gaps, honest partial
                   and unpriced states, and isolated failure at desktop and
                   390x844.
deviations:        Spine Decision 023 [P2.4, P4.1] and Harness Decision 026
                   [P2.4, P2.5, P4.1] journal the smallest implementation of
                   enacted Garden A-028/A-029. No plugin loader, parameter
                   system, lifecycle writer, browser-side accounting, or
                   per-request materialized-view refresh was added.
                   The live phone pass found two non-blocking friction notes:
                   long model identities ellipsize visually at 390px although
                   the full accessible identity and trace remain exact; the
                   isolated verification browser profile retained older
                   verification-only thread cards although its disposable
                   backend home contained only this run. Both are preserved in
                   the SOP for later taste/procedure refinement; neither is an
                   accounting or isolation defect. Full Spine `ruff check .`
                   passes and every M2C Python path passes `ruff format
                   --check`; the repo-wide formatter still names three
                   untouched pre-existing files, which this packet did not
                   churn.
evidence:          Spine ac332d2; Harness 3f2f6ed; Garden A-028 edb9441 and
                   A-029 97950a9. Final locked suites: Spine 181 passed;
                   Harness 576 passed / 3 live-contract tests deselected; web
                   lint clean, 6 unit tests passed, production build clean.
                   B.6 rule 7 reran green after the final build with scripted
                   screenshots and rendered/trace JSON under
                   `verification/m2c/`; adversarial states cover partial,
                   all-unpriced, empty, Vitals-only failure, collapse/reopen,
                   mobile overflow, selection serialization, all 13 gauges,
                   and A-029 collisions. Three independent read-only audits
                   returned PASS after the evidence corrections.
                   B.6 rule 8 used the real owner daemon, current disposable
                   Spine/PostgreSQL, real OpenRouter chat and embedding, and
                   an isolated browser profile. `verification/m2c/SOP.md` and
                   `live-trace.txt` correlate the same thread/run from visible
                   `1 req · 915 in · 184 out` through one embedding plus four
                   chat spend rows to the exact rendered minute totals
                   `$0.000000200000` and `$0.000464580000`; console errors and
                   warnings were zero. The temporary servers, database volume,
                   browser tab, home, and listeners were removed.
notes to the next agent:
                   M2G is next by deterministic board order; do not claim it
                   in this session. D3 remains separately blocked only on
                   owner creation of the two PyPI trusted publishers. Treat
                   `v_spend_rate` as derived minute-cadence data: Vitals reads
                   but never refreshes it. Preserve the public query/selection
                   seam and A-029 public model-key escape. The rule-7 fixture
                   is intentionally bannered and distinct-port only; rule-8
                   and later judge re-executions must continue using real
                   broker calls. The two live visual/procedure friction notes
                   above are taste follow-ups, not permission to widen M2G.
