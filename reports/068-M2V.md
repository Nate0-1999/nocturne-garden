packet:            M2V — Editable-checkout web assets + readiness voice
session:           codex / 2026-08-06 / 7b4e
status:            DONE
what exists now:   `harness.packaged:create_app` resolves wheel `_web` first,
                   then canonical editable-checkout `web/dist`; a cold source
                   checkout builds once through the existing Node path.
                   Missing/unbuildable assets stay reachable as one plain,
                   actionable 503, and `nocturne up` stops on its first body
                   instead of producing a readiness-poll wall.
                   The inherited post-M2Q test-citation regression is repaired:
                   the mechanical gate now reports zero grandfathered tests.
deviations:        Harness Decision 043 [P4] — serve the canonical build in
                   place rather than synthesizing an ignored package copy;
                   treat a permanent local refusal as actionable terminal state.
evidence:          Harness 2ae09fe. `harness/verification/m2v/README.md` records
                   the cold acceptance with both `web/dist` and `_web` absent:
                   the real packaged factory rebuilt 287 modules and served
                   HTTP 200 HTML; the generated build matched the committed one.
                   Focused regressions: 66 passed. Final ground: Spine 222
                   passed; Harness 658 passed, 3 deselected; Ruff passed;
                   motivation checker 376 tests / 0 grandfathered; web ESLint,
                   7 unit tests, TypeScript, and Vite build passed.
notes to the next agent:
                   Boot fresh and claim M2U. Do not claim M2X: it is HUMAN and
                   remains dependency-blocked until M2U is DONE.

                   M2U remains narrow: replace the two fixture-era embedding-
                   model expectations with provider-aware expectations, keep
                   the deterministic fixture path green, and pass all three
                   live contract tests against both fixture and deployed
                   Palace. Do not reopen M2T/M2V or run another deploy.

                   No new glossary term is proposed.
