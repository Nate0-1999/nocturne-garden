packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / 4d2c
status:            DONE
what exists now:   Harness commit 2b0c020 closes M2N's last migration-safety
                   gap. Before owner-cloud Alembic, the fixed-target deployer
                   creates one uniquely described Cloud SQL on-demand backup,
                   waits on its exact operation for at most 30 minutes, and
                   independently describes the exact backup as SUCCESSFUL.

                   Only then does it atomically persist a private schema-v1
                   receipt under NOCTURNE_HOME/cloud-backups and begin the
                   packaged migration. Any cloud verification or receipt-write
                   failure stops before the proxy and Alembic. The receipt is
                   evidence and a provider locator only: M2N adds no automated
                   cloud restore and no cloud-backup deletion authority.

                   M2N is complete across reports 053–058: pinned database
                   image, config migrations, serialized Alembic, historical
                   upgrade matrix, verified retained local backups, doctor,
                   startup/resource watch, bounded soak, informed side-by-side
                   restore, and local plus owner-cloud pre-migration receipts.
deviations:        Garden A-046 [P1.3, P4] and Harness Decision 038 define the
                   exact provider completion check and private receipt. This
                   fills the packet's evidence silence without expanding cloud
                   recovery authority. No vision deviation and no new glossary
                   term.
evidence:          Fresh boot ground passed before the slice: Harness 626
                   passed / 3 live contracts deselected; Spine 221 passed;
                   web ESLint, 7 Node tests, TypeScript, and Vite build passed.

                   Post-change: Harness 628 passed / 3 live contracts
                   deselected; Spine 221 passed through the mandated Colima
                   socket override; web ESLint, 7 Node tests, TypeScript, and
                   Vite build passed. Full Harness Ruff lint and changed-file
                   formatting passed. Motivation check passed at 350 tests /
                   307 grandfathered and law coverage was regenerated.

                   The focused deploy/CLI suite passed 143 tests. Its ordering
                   regression asserts receipt existence inside the migration
                   command and its failure regression proves a mismatched
                   provider backup publishes no receipt and never reaches
                   Alembic.

                   verification/m2n/cloud-backup-receipt.md records the live
                   provider trace: on-demand backup 1785900577396 completed
                   SUCCESSFUL in us-central1; the exact operation, backup,
                   description, and receipt IDs agree; the 19-field local
                   receipt persisted at modes 0700/0600. The live proof did not
                   invoke migration, restore, delete, or alter service traffic.
notes to the next agent:
                   Run a fresh boot. M2N is DONE; do not reopen its data-loss
                   boundaries unless fresh evidence finds a defect. Cloud
                   restore remains a human Cloud SQL operation, and the live
                   on-demand backup plus private receipt should be retained.

                   Board order now makes M2Q the next deterministic packet;
                   M2P remains later. D3 remains independently blocked only on
                   the owner's two PyPI trusted-publisher registrations.
