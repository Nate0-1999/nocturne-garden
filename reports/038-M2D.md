packet:            M2D — Durable transcripts (capture-only)
session:           codex / 2026-08-02 / fe58
status:            DONE
what exists now:   The production Harness daemon writes one private,
                   append-only standard-JSONL file per hashed thread beneath
                   `$NOCTURNE_HOME/transcripts`. User and assistant snapshots
                   carry ADR-016 `parentId` plus an explicit logical tail;
                   daemon-authored C.7 events are fsynced before delivery even
                   while detached. Same-thread ingress is capture-ordered,
                   queued parents are corrected without moving the tail
                   backward, and restart continues from the durable tail id
                   without hydrating UI/process state. Root and leaf no-follow
                   opens, 0700/0600 modes, locked complete-record appends,
                   partial-write rollback, invalid-tail recovery, and fatal
                   capture poisoning enforce the local durability boundary.
deviations:        Harness Decision 025 [P3] records the capture-first JSONL
                   format, logical-tail marker, single-daemon/single-journal
                   ownership, and fail-closed run-loop behavior. No Garden
                   amendment was needed. M3 session tables, transcript serving,
                   query, rewind, and backfill were deliberately not built;
                   snapshot and memory-panel replies remain serving artifacts.
evidence:          Harness e44bfaa6e7cf6137531ee68bb392b281e3b56458;
                   `verification/m2d/README.md` maps the packet law to code and
                   regressions. Final locked Harness suite: 549 passed / 3
                   live-contract tests deselected; Ruff, format, and diff
                   checks green. Final locked Spine suite: 174 passed. Three
                   independent adversarial reviews found no remaining blocker
                   after fixes for restart roots, resolver/FIFO reordering,
                   poison escape, partial writes, root/leaf redirection, and
                   logical-tail regression.
notes to the next agent:
                   M3 owns loading/backfilling these rows and every session,
                   tree, query, and rewind surface. `tail_message_id` is the
                   durable continuation pointer; physical JSONL order also
                   contains immutable revisions and must not be mistaken for
                   logical conversation order. File locks protect record
                   integrity if another local writer touches a file, but
                   semantic scheduling intentionally remains one daemon and
                   one journal. There is no UI change or reload requirement in
                   M2D. M2E is now next by deterministic board order; this
                   session does not claim it.
