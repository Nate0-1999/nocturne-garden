packet:            D1 — GCP deploy & remote verify
session:           codex / 2026-07-20 / 7f3a
status:            RETURNED_TODO
what exists now:
  - Cloud SQL `n8-memory-palace-db` now has the `spine` database/user, backups,
    seven-day retention, PITR, deletion protection, and Alembic `0002 (head)`.
  - Three region-pinned Secret Manager secrets and dedicated `spine-runtime`
    identity exist; runtime IAM is only Cloud SQL Client plus per-secret
    Secret Accessor.
  - Immutable Artifact Registry image for Spine SHA `3b4b80314bb2376a961168d56a2114287546ad90`
    is deployed to one Gen2 Cloud Run service, revision
    `n8-memory-palace-spine-00002-2bs`, at max scale 1.
  - Cloud Run reports Ready/RoutesReady and 100% traffic, but both advertised
    URLs return Google's edge HTML 404 and no request reaches the container.
deviations:
  - Spine Decision 015 (P4) records regional secrets, immutable local Buildx
    delivery, dedicated least-privilege runtime IAM, and operator-only
    migration.
  - The Auth Proxy had no ADC, so migration used a short-lived token from the
    already-active gcloud deployer; there was no login, ADC install, or wider
    authority.
  - The relay reasserted default-URL and traffic configuration, then retried
    the identical immutable deployment once. Both revisions were healthy; the
    edge remained 404. No second service/region, load balancer, custom domain,
    broad IAM, delete, GCS, Cloud Build, billing, or D2 mutation occurred.
evidence:
  - `spine/verification/d1/README.md`
  - `spine/verification/d1/cloud-state.json`
  - Spine `160 passed in 5.12s`; Harness `220 passed, 2 deselected in 1.25s`.
  - Both repos passed Ruff, formatting, lock, whitespace, and evidence JSON
    checks.
  - Spine evidence/journal commit `3aa6697`; Garden claim commit `15f1205`.
notes to the next agent:
  - First probe authenticated `/healthz` on both service URLs:
    `https://n8-memory-palace-spine-713925718873.us-central1.run.app` and
    `https://n8-memory-palace-spine-7wq3wmgcoq-uc.a.run.app`.
  - If unauthenticated health is the app's 401 and bearer health is 200, run
    create → hard duplicate → prepare → commit with a unique principal/project,
    update only ignored local `harness/.env` `SPINE_URL`, run the typed Harness
    remote smoke, append successful redacted evidence, and finish D1.
  - If the hosts still return edge 404 with no Cloud Run request log, stop and
    escalate this existing service/route to the human or Google Cloud support.
    Do not create a replacement service/region or widen IAM.
  - The ignored local `SPINE_URL` was intentionally not changed. All temporary
    files containing the generated DB credential, DB URL, app token,
    OpenRouter key, or short-lived access token were removed.
  - H5 remains blocked until D1's real broker-backed round trip and Harness
    remote smoke pass.
