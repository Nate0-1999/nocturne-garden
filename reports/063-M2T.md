packet:            M2T — Owner-cloud alignment + deploy + one-step update
session:           codex / 2026-08-06 / c807
status:            BLOCKED
what exists now:   Harness 8f3e913 wires credential alignment inline into
                   ordinary deploy, carries `nocturne up`'s single update
                   consent into that path, and orders the owner sequence image
                   → service → migration → authenticated verification. It also
                   accepts gcloud's real PostgreSQL built-in user shape while
                   continuing to reject IAM users and duplicates.

                   The literal owner path reached one `nocturne up → y`; its
                   dry-run correctly read Alembic 0002 and proposed image,
                   service, migration, and verification updates. It then failed
                   before backup or mutation because editable Spine lacked the
                   wheel-only deploy-resource directory. Spine f060cf6 fixes
                   that source seam with the same allowlist. Harness 3d1b6ca
                   adds the durable, private, non-secret custody receipt needed
                   to make the expressly granted reset happen once even though
                   the inherited credential currently authenticates.
deviations:        Harness Decision 041 now records the exact inline consent,
                   same-attempt receipt reuse, and durable custody boundary.
                   Spine Decision 032 records identical source materialization
                   from wheel or canonical checkout. F017 stops the retry:
                   D.2 096 was consumed by the failed approved run as written.
evidence:          Harness: 645 non-live tests passed, Ruff clean; web ESLint,
                   seven Node unit tests, TypeScript, and Vite build passed.
                   Spine: 222 passed with the required Colima settings, Ruff
                   clean. Focused one-step tests prove one consent, decline
                   safety, exact image→service→migration→verification order,
                   same-attempt receipt reuse, and `up` consent forwarding.

                   Live read-only proof after failure: latest receipt remains
                   01KZA98YYHDRZWTBTRQ7SNVZTS from the prior D.2 094 attempt;
                   `cloud-credential-custody.json` is absent; database secret
                   version 1 remains sole enabled; Artifact Registry has no
                   `0.1.0` tag. No password or secret value was printed.
notes to the next agent:
                   Do not replay `nocturne up → y` until the owner resolves
                   F017 with another exact single-use grant. The pre-mutation
                   path is now proven locally and both repairs are pushed.

                   On re-grant, fresh boot and pull all three repos. Run the
                   literal `nocturne up --no-open`, answer one `y`, and let that
                   same run do fresh receipt → corrected reset → secret rewrite
                   → image → service → migration 0002→0009 → health + typed
                   round trip + `/v1/vitals`. After the daemon starts, stop it
                   cleanly, inspect only non-secret receipts/state, close M2T,
                   and leave M2X HUMAN-held.
