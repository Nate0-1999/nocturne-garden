packet:            S3 — Scorer v0 + inject/prepare
session:           codex / 2026-07-19 / b73a
status:            DONE
what exists now:   Spine POST `/v1/inject/prepare` is live: it embeds one
                   prompt, stamps a one-shot repeatable-read thread snapshot,
                   uses pgvector's raw-cosine top-50 plus the pure six-feature
                   v0 scorer, applies pins / threshold / top-k / greedy token
                   budget / near misses, and returns exact scored cards.
                   Every returned card has an atomic event with frozen `_memory`
                   content; injected cards alone advance stats and append a CAS
                   revision. Shared vector, ULID, and cl100k helpers avoid S2/S3
                   drift. Commit, feedback, and search remain named 501 stubs.
deviations:        DECISIONS 007 (P1.2) and 008 (P1.2, P1.3); enacted Garden
                   A-007 and A-008 for deterministic scorer mechanics and the
                   smallest coherent M1 snapshot/event/counter semantics.
evidence:          Spine `verification/s3/README.md`; 71 pytest checks passed
                   against disposable pgvector Postgres; independent `.732`
                   hand calculation, frozen-snapshot interleaving, two-thread
                   no-lost-CAS race, exact event/wire parity, request failures,
                   Ruff lint/format, uv lock, scope fence, Compose config, and
                   production Docker image passed. Harness: 27 passed, one
                   intentional H2 skip; Ruff lint/format and scope fence passed.
notes to the next agent:  S4 is next. Prepare is deliberately one-shot because
                   C.6 permits one M1 injection/thread and C.2 history cannot
                   reconstruct old vectors. Repeated prepare returns RFC7807
                   409. Render commit from each event's `features._memory`
                   snapshot plus `memory_kind`, never from the mutable head;
                   `score` is already quantized to C.2 REAL before ranking.
                   Events exist for pinned/injected/near-miss cards only. A
                   zero-card prepare therefore has no durable injection row:
                   C.4 gives no batch table or empty-commit rule, so S4 must
                   enact the smallest qualifying completion before defining
                   unknown/empty `injection_id` behavior. Stats writes are real
                   CAS revisions (`system:inject`, request machine, reason
                   `inject/prepare`) and thus move `updated_at` under A-001.
                   The active `scorer_config` row is the indivisible runtime
                   weights/params/version source; exactly one must be active.
                   Spine remote head: f2e1215284539192d9e437643431e4007fdad59f.
