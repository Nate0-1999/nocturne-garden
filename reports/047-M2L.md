packet:            M2L — Citation heuristic v1 (n-gram)
session:           codex / 2026-08-03 / a83f
status:            DONE
what exists now:   Harness deterministically detects lexical reuse of every
                   memory selected for an ordinary model call. It compares
                   only the successful final assistant text against exact
                   selected memory bodies, using lowercase Unicode-
                   alphanumeric token runs and an eight-token n-gram (or the
                   complete body for four-to-seven-token memories). Thinking,
                   prompts, tools, labels, failed runs, and bodies shorter than
                   four tokens do not manufacture citation signals.

                   The detector runs inside M2G's existing model/feedback lock
                   and uses the exact event source captured for that message.
                   First-turn reuse therefore labels the committed gate batch;
                   every later turn labels its autonomous re-scoring batch.
                   Each matching memory submits one existing cited feedback.
                   A Spine failure remains visible as memory_unavailable phase
                   citation without retracting an already-produced answer.

                   Spine now accepts cited from kept, added_back, and
                   auto_entered outcomes. The first transition atomically sets
                   the event outcome, increments stats.citations, and appends
                   the canonical C.2 revision; identical retries are no-ops.
                   f_freq is no longer inert.
deviations:        Garden A-036 completes the reproducibility facts omitted by
                   ADR-005: normalization, n, short-body behavior, successful-
                   output boundary, per-message attribution, failure behavior,
                   and the exact citation statistic transition. The chosen
                   detector is deliberately lexical and conservative. It adds
                   no LLM judge, semantic paraphrase claim, retry queue, batch
                   endpoint, UI, or scorer-weight update.
evidence:          Harness 216888b and Spine 2974d81 are pushed to origin/main.
                   Harness passes 590 non-live tests and Ruff. Its three
                   external live-contract tests remain intentionally excluded
                   because this session has no SPINE_URL/SPINE_TOKEN. Web
                   ESLint, six unit tests, TypeScript, and Vite build pass.
                   Spine's locked Colima suite passes 204 tests and Ruff.

                   Focused evidence covers Unicode/punctuation normalization,
                   exact short-body handling, first-gate and autonomous event
                   attribution, visible fail-open behavior, auto_entered to
                   cited, exactly-once citation counts, and revision provenance.
notes to the next agent:
                   M2M is next by deterministic board order; do not claim it in
                   this session. Preserve A-036's narrow evidence claim: an
                   n-gram match is lexical reuse, not proof of semantic
                   dependence. OQ-5's LLM-judge upgrade remains later work.
                   D3 remains blocked solely on the owner-created PyPI trusted
                   publishers.
