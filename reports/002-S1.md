packet:            S1 — DB layer & CAS rules
session:           codex / 2026-07-17 / 5f1c
status:            DONE
what exists now:   Spine commit 5da2e80 maps all five authoritative C.2 tables
                   with exact PostgreSQL types, defaults, checks, keys, and
                   indexes. One allowlisted helper performs head CAS + resulting
                   revision append under an internal savepoint; stale writes
                   carry a detached current-head 409 snapshot. Tombstoning uses
                   the same path, preserves the row, and releases active labels.
deviations:        Garden A-001 [P1.3] completes the previously silent revision
                   snapshot and updated_at semantics. Spine DECISIONS 004 [P1.3]
                   records the caller-owned transaction, validated client ULID,
                   derived parent lineage, savepoint, and S2 HTTP boundary.
evidence:          spine/verification/s1/README.md: Ruff and scope fence pass;
                   25 Postgres-backed tests prove zero model/migration drift,
                   defaults/checks, 1→2→3 parent_uid lineage, a forced concurrent
                   current-head 409, DB- and application-error rollback, caller
                   rollback, tombstone/no-delete, and label reuse. Harness remains
                   green at 27 pass + 1 intentional H2 skip; web lint/build pass.
                   Two independent final audits found no remaining S1 defect.
notes to the next agent:  Dispatch stops here. S2 is next by row order; H1 is
                   also dependency-ready but must wait for a fresh session.
                   S2 create must insert the revision-1 root beside memory_unit
                   in one transaction, or later CAS correctly raises lineage
                   corruption. Call cas_update_memory_unit only inside an explicit
                   `sessions.begin()` and at most once per memory in that outer
                   transaction; supply a canonical 26-character ULID. Map
                   MemoryCasConflictError to C.4's exact current-unit 409. Active
                   label violations remain PostgreSQL constraint errors for S2 to
                   identify and translate; S1 intentionally did not wire CRUD.
                   On this workstation use `PYTHONPATH=src` when File Provider
                   re-hides editable-install .pth files, plus the existing Colima
                   testcontainers socket override.
