packet:            M2Q — Test motivation sweep + law-coverage report
session:           codex / 2026-08-04 / c7e2
status:            RETURNED_TODO
what exists now:   Harness commits 50c2c0a + 92d7a45 and Spine commits
                   4a09b24 + 568d53d ship the same standalone deterministic
                   motivation checker, local
                   pre-commit hook, CI gate, syntax-digest grandfather
                   baseline, focused checker tests, and generated inverse
                   law-coverage report. Harness also motivates all six current
                   Node test calls with immediately preceding JSDoc.

                   The ratchet discovers 329 Harness tests and 151 Spine tests.
                   It accepts 17 and 4 already motivated tests respectively,
                   grandfathers 312 and 147, and has zero stale exemptions.
                   New, renamed, or source-modified tests now fail unless their
                   motivation prose cites enacted law. The report catalogs
                   SPEC/ADR headings, lists exact defenders, highlights zero-
                   defender headings, and separately indexes other statutes.

                   The full semantic reading sweep is intentionally not
                   claimed: 459 legacy tests still need individual motivation
                   review, then both baseline files must be retired and both
                   reports regenerated. PLAN explicitly permits this two-
                   session handoff.
deviations:        Garden A-040 completes SPEC B.6 rule 12 [P4]. SPEC v2.48
                   accidentally appends the rule to D.4's final parked-idea
                   sentence as `rankings.12.` even though D.2 088 and M2Q call
                   it B.6 law. A-040 restores its declared location and freezes
                   Python/JavaScript motivation forms, exact citation grammar,
                   cross-runtime normalized-source digests, and report scope.
                   Harness Decision 032 and Spine Decision 027 reject generic
                   generated docstrings and a cross-checkout shared script.
evidence:          Harness standard ground: 603 passed, 3 live contract tests
                   deselected; Spine: 210 passed through Colima/Testcontainers.
                   Web ESLint, 6 Node unit tests, TypeScript, and Vite build
                   passed. Full Ruff lint passed in both repos. Each checker
                   has 2 focused passing tests, and the checker passes under
                   both system Python 3.13 and locked Python 3.12 with identical
                   baselines. `git diff --check` passed in all three repos.

                   An initial raw Harness pytest correctly errored only because
                   the three live contract tests require SPINE_URL; the standard
                   non-contract command passed. Full Harness Ruff format check
                   reports six untouched HEAD files that Ruff 0.15.22 would now
                   reformat; the M2Q files themselves pass format checking.
                   This pre-existing formatter drift was not mixed into the
                   motivation packet because changing those legacy tests would
                   correctly trigger the new ratchet.
notes to the next agent:
                   Run a fresh boot; M2Q remains the first eligible TODO row.
                   Do not regenerate either baseline as a shortcut. Continue
                   THE SWEEP by reading each grandfathered test against its
                   governing law, adding one or two honest sentences with an
                   exact citation. If a test has no defensible statute, propose
                   the required completion or delete it with a journal entry;
                   do not paste a generic token. Use the baseline lists as the
                   work queue and regenerate `verification/law-coverage.md`
                   after each repository reaches zero debt. Delete both
                   baseline files only after the checker passes without them.
                   Then resolve the six-file Harness formatter drift in the
                   same reviewed edits (or journal why not), rerun all standard
                   gates, and close M2Q. Do not begin M2P in that session.
