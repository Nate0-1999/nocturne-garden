packet:            M2I — Seed ingestion
session:           codex / 2026-08-03 / 92ad
status:            DONE
what exists now:   The law-bound Palace Queue accepts one or more Markdown
                   files, treats each document as an independent corpus-born
                   batch, and runs a real tools-free model splitter over the
                   complete source before any queue write. Every child must be
                   independently comprehensible, keyworded, and inside the
                   standing 128-token limit. The UI supports `.md` and
                   `.markdown` only, caps each document at 24 KiB, and offers
                   explicit whole-batch approve or reject actions. Seed batches
                   never appear in thread-end cards and never passive-approve.

                   Spine reuses M2H's candidate create/dedup and verdict
                   mechanics. It stores the exact Markdown as one queue-
                   invisible tombstoned-as-split source head, points each
                   admitted child's first revision to the source revision, and
                   stamps every admitted sibling pair with symmetric
                   `relates_to` edges. Queue cards now carry an honest thread or
                   seed birthplace plus document batch metadata. Batch decisions
                   lock and decide every child in one transaction; identical
                   retries are idempotent and conflicts roll back the group.
deviations:        Garden A-033 closes M2H's thread-only birthplace and
                   item-only decision seams. It fixes Markdown-only bounds,
                   batch retry identity, exact-source lineage, symmetric sibling
                   kinship, and atomic explicit batch decisions. No document
                   store, filesystem copy, second staging path, passive seed
                   approval, expiry, notification, non-Markdown parser, or M3
                   curator machinery was added.

                   The in-app browser's file-chooser event did not propagate
                   from the sandboxed cross-origin rack iframe. The visible
                   chooser was unique and clickable, but automated file
                   selection could not complete. This is recorded in the M2I
                   SOP rather than counted as a pass. The live Harness service
                   test covers upload through the same route; the browser
                   fixture was seeded through localhost POST `/v1/seeds` so the
                   rendered grouping, scope, explicit decision, and responsive
                   states could still be exercised end to end.
evidence:          Spine f3b3e53 and Harness 456397f are pushed to origin/main.
                   Final locked suites: Spine 199 passed; Harness 582 passed
                   with the separately deployed live-contract tests ignored;
                   both Ruff suites pass. Web ESLint passes, six unit tests
                   pass, and the TypeScript/Vite production build passes with
                   committed dist. Spine migration 0007, metadata diff,
                   OpenAPI, lineage, birthplace filtering, symmetric edges,
                   batch idempotency, and atomic decision tests pass.

                   The visibly bannered M2I fixture ran on isolated port 8775.
                   At 1440x900, one Markdown document rendered as one two-memory
                   batch with the complete semantic children and only explicit
                   batch actions. At 390x844, the upload surface, both actions,
                   both children, and their bodies remained readable without
                   horizontal overflow. CURRENT scope persisted. Approving the
                   batch returned the quiet empty state; the fixture trace
                   recorded two approve/explicit/human decisions and pending=0.
                   Screenshots and the SOP are committed under
                   `verification/m2i/`.
notes to the next agent:
                   M2J is next by deterministic board order; do not claim it in
                   this session. It edits the SPA and should preserve the
                   Palace Queue as a separate law-bound corpus surface while
                   adding the parameter registry/model device. D3 remains
                   blocked solely on the owner-created PyPI trusted publishers.
                   The M2I chooser automation limitation is evidence tooling
                   friction, not proof that native user file selection fails;
                   recheck it when the browser runtime gains iframe chooser
                   support instead of adding a product workaround.
