packet:            M2P — Injection Console: audition + what-if
session:           codex / 2026-08-04 / 4f6a
status:            DONE
what exists now:   Spine commits 5ef0be4 and ffb4142 expose bearer-protected
                   instant/deep scorer simulation and read-only proposal
                   audition. Exact-value FORCE recomputes a content-addressed
                   receipt under the existing advisory activation lock and
                   journals its evidence with the immutable scorer version.

                   Harness commit bfe9308 carries simulation, force, and
                   audition only through the public Rack action bridge. The
                   Injection Console previews knob changes, shows held-out
                   accuracy and a two-dimensional slice, invalidates receipts
                   on edits, and fans audition marks to the live Gate and
                   Memory Panel as presentation-only state.
deviations:        Spine Decision 030 and Harness Decision 039 adopt Garden
                   A-047/A-048 [P1.2.3, P2.5]. A-047 completed the previously
                   undefined public replay/receipt contract. A-048 corrected
                   its impossible request for nine distinct `top_k` points:
                   the legal bounded integer domain contains eight.
evidence:          Spine full Testcontainers suite: 221 passed with the
                   mandated Colima socket override. Harness standard ground:
                   629 passed, three live SPINE_URL contracts intentionally
                   deselected. Web ESLint passed; all seven Node unit tests
                   passed; TypeScript and the production Vite build passed.

                   `verification/m2p/SOP.md` was executed in the in-app
                   browser against the visibly bannered isolated fixture on
                   port 8786. It proved CURRENT instant score/disposition
                   change, GLOBAL no-fabricated-gate behavior, DEEP 92% / +4
                   receipt and accuracy curve, edit-time FORCE invalidation,
                   exact FORCE version replacement, read-only learner
                   audition, zero browser warnings/errors, and mobile width
                   conservation at 390/390. Evidence is in
                   `verification/m2p/01-deep-current-desktop-1440x900.png`,
                   `02-audition-desktop-1440x900.png`,
                   `03-deep-current-mobile-390x844.png`, and `rendered.json`.
notes to the next agent:
                   Run a fresh boot. M2P is DONE and no deterministic M2 build
                   packet remains. D3 is still independently BLOCKED on F014:
                   the owner must create both PyPI trusted publishers before
                   release work can resume. Do not reinterpret that stop-line
                   or release from long-lived credentials.

                   M2P replay deliberately reuses M2F hygiene, actor weights,
                   whole-gate holdout, and binary referee. Selection-only
                   controls therefore have honestly flat deep curves; their
                   immediate consequence belongs to INSTANT. Legacy gates
                   without `_prepare.model_context_tokens` return
                   `not_replayable` rather than an estimate.
