packet:            M2B — Rack refound + NEO-NOIR identity
session:           codex / 2026-08-01 / c8e4
status:            DONE
what exists now:   Harness is a bounded twelve-unit CSS-grid rack whose Header,
                   Threads, Chat, Memory, and Gate are first-party modules on
                   one serialized public bridge. Every module runs in a
                   sandboxed `rack.localhost` iframe; manifests scope C.7
                   streams/actions and expose query+as_of, selection, and
                   ResizeObserver rectangles. Users can resize/dock in whole
                   units and save/restore the current set. NEO-NOIR is the
                   default token identity; 390×844 uses explicit drawers.
deviations:        Harness Decision 024 [P2.5] records the isolated-origin
                   MessagePort bridge, origin-pinned handshake, frame CSP,
                   twelve-unit neighbor-trading resize, and one saved-set
                   completion. No Garden amendment was needed. M2C, M2J, and
                   M3 surfaces were deliberately not stubbed or prepared.
evidence:          Harness 05d4e981283bf96dace93004e19a6872822bb55a;
                   `verification/m2b/README.md` maps the B.6 rule-7 run;
                   `verification/m2b/SOP.md` is the executed independent
                   rule-8 walkthrough. Scripted desktop 1440×900 and exact
                   390×844 runs prove rendered grid bounds, pointer resize,
                   keyboard/drag docking, save/restore/reload, drawer states,
                   Gate Continue, scroll range with fixed actions, CSP walls,
                   and forged-origin bridge refusal; browser diagnostics are
                   clean and all seeded H5 memory IDs were tombstoned. The
                   live 1280×720 + 390×844 pass additionally exercised odd
                   whitespace, a new thread, both drawers, Continue, and Stop.
                   Final Harness suite: 532 passed / 3 live-contract tests
                   deselected; Ruff, format, web lint/build, and 3 rack-layout
                   tests green. Spine final suite: 174 passed.
notes to the next agent:
                   Host state has one owner: the existing Zustand/WebSocket
                   adapter. Rack residents never import it or `harnessClient`;
                   they receive serialized messages over the origin-checked
                   bridge. The H5 fixture still points at the deployed Spine
                   revision that predates M2A's spend-receipt endpoint, so a
                   live Continue shows the model response and then honestly
                   terminalizes with “Spend receipt could not be persisted.”
                   That is report 036's undeployed-M2A boundary, not a rack
                   failure and not proof of remote spend persistence. The
                   live phone wheel gesture was visually inconclusive and is
                   recorded as such; the final scripted run measured the Gate
                   body moving 0→1345px while Continue remained visible.
                   M3 still owns arbitrary plugin folders/public SDK/hot
                   reload; M2J owns parameter bindings; M2C owns the spend
                   strip. The next deterministic claimable row is M2D; this
                   session does not claim it.
