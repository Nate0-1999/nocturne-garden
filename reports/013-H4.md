packet:            H4 — Web shell + chat
session:           codex / 2026-07-20 / c84d
status:            DONE
what exists now:   Harness serves a responsive React chat shell through the
                   production daemon and one same-origin WebSocket. A local
                   UUID thread catalog, authoritative snapshot replacement,
                   incremental text/thinking/event rendering, cumulative
                   usage, turn-boundary queue, confirmed Stop, distinct
                   cancelled/budget/error partial states, reconnect hydration,
                   and 1013 backpressure recovery are implemented. Desktop is
                   a thread rail plus quiet chat pane; 390×844 uses one chat
                   pane and a keyboard-operable full-height thread view.
deviations:        Garden A-017 completes H4 message rows, browser-local thread
                   metadata, direct-link machine identity, and 1013 recovery.
                   Garden A-018 keeps pre-ack prompt text in a volatile overlay
                   so overlapping attach/request snapshots cannot erase it
                   while every snapshot remains authoritative. Harness
                   DECISIONS 011 (P2/P3) records the direct shell, state/socket
                   ownership, visual restraint, and deliberate system-font
                   fallback because no licensed font assets are committed.
                   No FLAG, Spine production change, H5 gate, H6 panel, Cube,
                   M3 controller, replay, polling, or server thread list.
evidence:          Harness verification/h4/README.md and trace.jsonl; paired
                   1440×900 and 390×844 screenshots cover empty, streaming,
                   queued, rehydrated, cancellation, and terminal boundaries.
                   Browser checks found no overflow or console errors and
                   verified 44px controls plus mobile focus/Escape behavior.
                   220 non-contract tests pass (2 deselected), 43 loop/daemon
                   cases pass under asyncio debug with RuntimeWarnings fatal,
                   the live migrated Spine contract passes 2 tests with clean
                   teardown, and inherited sibling Spine ground remains 158
                   passing tests. Ruff, format, uv lock, M1 scope, web
                   lint/build, trace assertions, diff checks, and three
                   independent re-audits are green.
notes to the next agent:  Harness remote head is
                   33f85faf55d8fdf84f5e02809a97eb3430be7c50. Consume
                   web/src/protocol.ts, store.ts, and socket.ts rather than
                   duplicating C.7 parsing or state. Snapshot replacement is
                   intentionally wholesale; only volatile unacknowledged
                   prompts are projected outside it, never persisted or
                   resent. Thread titles/catalog entries are navigation
                   metadata, not server authority. The transcript is not an
                   aria-live region and auto-follow never yanks a reader who
                   scrolled away. H5 remains blocked on the human-owned D1
                   deploy gate despite H4 now being DONE; no other TODO packet
                   is dependency-ready. Stop here under the one-packet rule.
