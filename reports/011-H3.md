packet:            H3 — Agent + memory tools
session:           codex / 2026-07-20 / 7c3a
status:            DONE
what exists now:   Harness owns all five typed ADR-013 capability axes and a
                   sole pydantic-ai adapter. MemoryCapability is a vanilla v2
                   Capability with the verbatim instruction and exactly
                   save_memory, search_memory, and edit_memory. Trusted run
                   context supplies all identity/scope; save surfaces similar
                   and exact conflicts, search renders compact ordered cards,
                   and edit resolves exact active units then retries revision
                   conflict once. HarnessAgent provides bounded chat plus a
                   tools-free, one-label-call /remember service that confirms
                   only a created global user fact.
deviations:        Garden A-015 completes save_memory with force=False so the
                   specified similar-result retry is executable. Harness
                   DECISIONS 006 (P1.2), 007 (P1.4), and 008 (P1.2/P1.4)
                   record the owned adapter seam, trusted tool mappings, exact
                   dependency pin, provider boundary, and service-level
                   /remember dispatch. No FLAG and no daemon/Spine change.
evidence:          Harness verification/h3/README.md; 147 non-contract tests,
                   including 61 focused H3 cases, and 2 live contract tests
                   against migrated Spine. All 158 Spine tests pass. Ruff
                   lint/format, uv lock, M1 scope fence, diff check, and an
                   independent adversarial/re-audit are green with no blocker.
notes to the next agent:  Harness remote head is
                   31456a312a39449ef78bb80aa2893e50529eb9b5. H3 intentionally
                   did not wire daemon envelopes, the injection gate, or loop
                   controls: H7/H4/H5 own them. HarnessAgent.dispatch is the
                   callable seam; carry its opaque message_history forward.
                   Give each run a trusted MemoryToolContext backed by one
                   lifespan-owned Spine client. /remember is global, uses the
                   selected thread model through a separate zero-tool Agent,
                   and is hard-walled to one label request. Direct providers
                   are OpenRouter/Anthropic/OpenAI; route other models through
                   OpenRouter. pydantic-ai is pinned at 2.12.0; future
                   lifecycle/history/event adoption belongs only in the sole
                   adapter and only on first use. On Colima, Spine's test suite
                   needs TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock.
                   H7 is the next dependency-ready board packet. Stop here.
