packet:            M2G — Per-message re-scoring + disposition locks
session:           codex / 2026-08-03 / b803
status:            DONE
what exists now:   Spine implements A-030 autonomous prepare: every ordinary
                   post-gate message is freshly scored while confirmed and
                   pinned memories remain binary locks, thread-excluded
                   memories stay out, and the remaining budget is filled from
                   current candidates. Passive injection events are marked by
                   actor_class, preserve one batch across the prepare rows,
                   and accept exactly the panel feedback transitions needed
                   for remove, re-add, and later remove.
                   Harness now owns daemon-lifetime per-thread confirmed,
                   current, excluded, and event-source state. The first
                   message still uses the sole review modal; later ordinary
                   messages autonomously replace only the scored portion and
                   emit an ambient panel update with a daemon request id and
                   `rescored` result. Pinned or confirmed memories survive
                   rescoring, excluded memories expose re-add, and remember
                   commands retain their existing non-rescoring path. The
                   desktop and 390x844 owner surfaces render these states
                   without repeating the modal or crowding the mobile header.
deviations:        Garden A-030, Spine Decision 024 [P1.2, P1.4], and Harness
                   Decision 027 [P1.2, P1.4] record the smallest executable
                   interpretation of OQ-15. State is intentionally limited to
                   the daemon lifetime: no durable thread-state store,
                   Chrysopoeia learner, extraction queue, citation heuristic,
                   or new generalized orchestration layer was added.
                   Rule 8 found two product defects that rule 7 had not: the
                   scorer rejected a confirmed memory when it was also pinned
                   but absent from the ordinary lock pool, and feedback
                   rejected removal of an autonomously-entered memory. The
                   implementations and A-030 were corrected, with regression
                   tests for both paths. The phone pass also exposed redundant
                   connection chrome; the narrow layout now hides that label
                   while retaining the status control.
evidence:          Spine aab87ab; Harness 5a8338a; Garden A-030 7e5f1c8 plus
                   this closure. Final suites: Spine 185 passed and `ruff
                   check src tests` clean; Harness 577 passed / 3 live-contract
                   tests deselected and `ruff check src tests verification/m2g`
                   clean; web production build and lint clean, 6 unit tests
                   passed. B.6 rule 7 covers the first gate, repeated
                   autonomous scoring, lock persistence, exclusion, re-add,
                   removal after re-add, mobile layout, and event tracing in
                   `verification/m2g/` on the visibly bannered port-8783
                   fixture.
                   B.6 rule 8 used the real owner daemon on port 8765, current
                   local Spine/PostgreSQL, and real OpenRouter chat and
                   embeddings. The same live thread shows a first-message
                   modal, no later modal, a confirmed lock retained, a newly
                   pinned ambient memory entering the next autonomous context,
                   remove then re-add succeeding, correlated passive database
                   rows, and zero browser console warnings/errors. The two
                   exact seed memories were tombstoned; both temporary servers
                   and Compose services were stopped; the temporary owner home
                   and browser tabs were removed. The pre-existing Compose
                   volume was deliberately preserved.
notes to the next agent:
                   M2F is next by deterministic board order; do not claim it
                   in this session. M2L is now dependency-ready but remains
                   later in row order. D3 remains separately blocked only on
                   owner creation of the two PyPI trusted publishers. Preserve
                   the first-gate-only modal, binary lock semantics, explicit
                   excluded state, and the distinction between the rule-7
                   fixture and rule-8 owner product. Do not turn daemon-lifetime
                   thread state into persistence without a new packet and law.
