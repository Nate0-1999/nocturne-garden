packet:            M2N — Owner DB lifecycle hardening
session:           codex / 2026-08-04 / 6b1d
status:            RETURNED_TODO
what exists now:   Harness commit 000ed8b adds the read-only `nocturne doctor`
                   lifecycle surface. It reports database, conversation-journal,
                   backup, and filesystem-capacity measurements; warns at the
                   greater of 5 GiB or ten percent free; and returns distinct
                   healthy, warning, and failed exit statuses.

                   Doctor reuses the A-042 receipt authority rather than
                   creating another backup parser. Every recognized generation
                   must retain exact contents and schema, an aware UTC receipt,
                   private modes, matching size and SHA-256, and acceptance by
                   the pinned container's `pg_restore --list`. Unknown backup
                   entries remain untouched. The shared validator also makes
                   retention stricter about timestamp, exact contents, and
                   symlink rejection.

                   M2N remains incomplete. Informed side-by-side restore,
                   owner-cloud pre-migration backup receipts, the startup disk
                   warning, resource Vitals, and the daemon soak bound remain.
deviations:        Garden A-043 [P1.3, P4] and Harness Decision 035 define the
                   doctor checks and conservative early-warning boundary. The
                   boundary is diagnostic only; it does not implement the later
                   configured Deck threshold, delete data, or create a hard
                   stop. No vision deviation and no new glossary term.
evidence:          Fresh boot ground passed before the slice: Harness 611
                   passed / 3 live contract tests deselected; Spine 220 passed;
                   web ESLint, 6 Node tests, TypeScript, and Vite build passed.
                   Post-change focused tests: 14 passed. Full Harness Ruff lint
                   and all changed-file Ruff formatting passed. Motivation
                   check: 336 discovered / 309 grandfathered.

                   `verification/m2n/doctor-smoke.md` records a real isolated
                   PostgreSQL run: one backup published, doctor exit 0, a 7.3
                   MiB database measured, the archive reverified, and all
                   disposable container/network/volume/files removed.

                   The deterministic law-coverage regeneration and broader
                   post-change suite rerun were not completed: the execution
                   approval service rejected the next uv command because its
                   usage limit was reached. This is an environment limitation,
                   not a product failure; no broader-pass claim is made.
notes to the next agent:
                   Run a fresh boot; M2N remains first eligible by board order.
                   Do not duplicate `harness.lifecycle` receipt or resource
                   measurement logic. Regenerate `verification/law-coverage.md`
                   and run the full suites before eventual M2N closure.

                   Restore must remain literal v2.49: fresh side-by-side volume,
                   restore and verify there, compute the named rollback manifest,
                   explicit confirmation, then switch while retaining the former
                   volume. A failed restore never touches live state.

                   The owner-cloud seam still begins at
                   `GcloudDeployBackend._apply_migrations`; add a Cloud SQL backup
                   receipt before that call without pretending cloud restore is
                   automated. Finish startup disk warning, resource Vitals, and
                   the stated soak bound. Do not begin M2Q or M2P.
