packet:            M2T — Owner-cloud credential alignment + M2 deploy
session:           codex / 2026-08-06 / c811
status:            DONE
what exists now:   M2T is closed. One owner-authorized verification-only
                   `nocturne deploy` execution passed the corrected production
                   typed round trip, isolated cleanup, Vitals probe, and final
                   verification-label write. A post-run deploy observation
                   reports all 20 stages NOOP.

                   The production service now carries
                   `nocturne-verified=6ad9613a455798d6d92e5f5f390ab4ba`.
                   Cloud Run materialized revision 00006-b75 for that label
                   update; its complete runtime spec equals 00005-t64, it uses
                   the same resolved image digest, and it serves 100% healthy
                   default traffic.

                   Harness b617485 enforces the v2.55 scope boundary and
                   mutation-last ordering. Harness 9f3e500 preserves the full
                   redacted production closure evidence.
deviations:        none. The new Cloud Run revision is recorded explicitly as
                   the platform side effect of the specifically authorized
                   verification-label command; complete old/new revision specs
                   compare equal, so no image, secret, migration, or runtime
                   configuration changed. No new glossary term is proposed.
evidence:          Fresh Boot ground passed: Spine 222 tests and Ruff; Harness
                   654 tests with 3 contract tests deselected and Ruff; web
                   ESLint, 7 unit tests, TypeScript, and Vite build.

                   Pre-run dry-run: stages 01-18 and 20 NOOP, stage 19 UPDATE.
                   The single live command exited 0. Sanitized Cloud Run
                   request metadata records health 401/200, memory create 201,
                   duplicate 409, prepare 200, commit 200, isolated list 200,
                   cleanup PATCH 200, and `/v1/vitals` 200. Harness required
                   the cleanup response status to be TOMBSTONED before the
                   label could be written.

                   Post-run dry-run: all 20 stages NOOP. The private receipt
                   set remains four files, newest
                   01KZD3K0CVGV29JMG7QAF9TPZ5. The latest on-demand Cloud SQL
                   backup remains 1786072630287. Database secret metadata is
                   unchanged at v2 enabled / v1 disabled / no v3. Revisions
                   00005-t64 and 00006-b75 have identical full spec JSON and
                   resolve image digest
                   ec222889fb4af37b94c6b1d5382f91fed915dfce1a157ea3ec42939c27b59903.

                   Durable detail:
                   harness/verification/m2t/verification-only-closure.md.
notes to the next agent:
                   Boot fresh and claim M2U. Do not claim M2X: it is HUMAN and
                   remains dependency-blocked until M2U is DONE.

                   Production ground is revision 00006-b75 at 100% default
                   traffic, schema 0009, database secret v2/latest, image
                   0.1.0, and matching image/verification markers. A Cloud Run
                   service-label update can materialize an identical-spec
                   revision; compare runtime specs instead of assuming the
                   revision name stays fixed.

                   M2U's charge is narrow: replace the two fixture-era hardcoded
                   embedding-model expectations with provider-aware real
                   expectations, keep the deterministic fixture path green,
                   and make all three live contract tests pass against both
                   paths. Do not reopen M2T or run another deploy.
