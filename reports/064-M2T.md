packet:            M2T — Owner-cloud alignment + deploy + one-step update
session:           codex / 2026-08-06 / c808
status:            BLOCKED
what exists now:   The approved literal `nocturne up → y` attempt minted fresh
                   verified receipt 01KZD1HVSZXWXJGPNEN58PY6XC, consuming
                   D.2 097. It then reset the database credential, added and
                   enabled `spine-database-url` version 2, disabled version 1,
                   and published the private custody receipt linked to that
                   backup. No credential value was printed or retained.

                   The next stage failed at `docker buildx build` before any
                   image push. Root cause: the short-lived `DOCKER_CONFIG`
                   correctly excluded persistent registry credentials but
                   also hid Homebrew Buildx and the Colima context. Therefore
                   Cloud Run did not roll, Alembic remains 0002, and the M2
                   authenticated verification did not run.

                   Harness 4a5a4db fixes the local defect without replaying
                   production: the isolated config allowlists only Docker
                   routing/plugin facts, the exact isolated Buildx + daemon
                   environment is now preflighted before a receipt, and a
                   post-custody continuation still takes a fresh receipt then
                   orders image → service → migration → verification. Custody
                   prevents a second credential reset or secret rewrite.
deviations:        Harness Decision 042 records the Docker isolation boundary.
                   F018 stops all further mutation because D.2 097 was
                   consumed post-receipt. The service revision still pins the
                   now-disabled version-1 database secret: static `/health`
                   returns 200, but an authenticated DB-backed memory read
                   returns 500. The owner service is disrupted until an
                   authorized rollout adopts version 2/latest.
evidence:          Provider re-description: backup 1786070495876, operation
                   17e0291f-3034-4992-81db-cbe400000032, SUCCESSFUL ON_DEMAND
                   in us-central1. Backup directory is 0700; fresh receipt and
                   custody receipt are 0600. Secret metadata only: v2 ENABLED,
                   v1 DISABLED. Artifact Registry: no `0.1.0` tag. Cloud Run:
                   revision n8-memory-palace-spine-00004-vs2, old dfe9fd…
                   digest, 100% traffic, database secret key 1.

                   Corrected read-only real deploy observation exits 0 and
                   proves the version-2 credential reads Alembic 0002:
                   database/user/secret exact; migration UPDATE; image CREATE;
                   service UPDATE; remote verification UPDATE. Status-only
                   authenticated checks: `/health` 200, DB-backed memories
                   500, `/v1/vitals` 404. No bodies retained.

                   Harness: 141 deployment tests passed; 649 full non-live
                   tests passed with three live-contract tests deselected;
                   Ruff clean. Boot ground before the attempt: Spine 222
                   passed + Ruff clean; Harness web ESLint, seven Node tests,
                   TypeScript/Vite build passed. Full safe transcript is
                   `harness/verification/m2t/production-attempt.md`.
notes to the next agent:
                   Do not replay any cloud mutation until the owner resolves
                   F018. Pull Garden and Harness 4a5a4db first. The minimum
                   resolution is one fresh single-use POST-ALIGNMENT
                   COMPLETION grant: fresh verified receipt first; reset and
                   secret rewrite remain NOOP; image build/push → rollout →
                   migrations 0002→0009 → authenticated health, typed round
                   trip, and `/v1/vitals` verification. Run it through the
                   same owner experience, `nocturne up → y`, then close M2T.
                   Leave M2X HUMAN-held.
